# Changelog

Registo de alterações do **Nexus Infra**. Mais recente no topo.
Categorias: 🔒 Segurança · 🧰 Funcionalidade · 🎨 UI/Marca · 🧹 Limpeza · 🛠️ Infra
_(itens de infra vivem no servidor e não têm commit)._

---

## 2026-08-11

- 🔒 **Revisão de segurança (16.ª) + endurecimento** — auditoria ao diff desde a 15.ª (28 commits, 46 ficheiros: equipamentos "por associar" do sync, alertas programados com texto editável em contratos e equipamentos, remoção da prioridade dos SLAs, recibos embebidos no PDF das despesas, scanner de recibos, pesquisa/ordenação da listagem de equipamentos, `criado_erp_em`): **sem falhas críticas, altas ou médias**. Verificado em concreto: isolamento do portal intacto com equipamentos sem local (o scope do cliente resolve por `whereHas('local')` — local null nunca casa com cliente nenhum, fail-closed; relatórios e intervenções idem); CRUD dos alertas só em componentes `ApenasEquipa` (+ `abort_if` próprio no guardar), com validação (data obrigatória, texto ≤ 255, máx. 24 linhas); texto editável escapado em todas as vistas (`{{ }}`, sem `{!! !!}` novo no intervalo); o PDF das despesas embebe apenas anexos das linhas do próprio registo (relação morph; `storage_key` gerado pela app, mime dentro de `{{ }}`); pesquisa da listagem parametrizada (ilike com bindings) e ordenação por whitelist `match()` com raws constantes; driver ERP com query estática (`TOP` com cast) e `ousrdata`/`ousrhora` normalizados por Carbon/regex antes de tocar na BD; scanner 100% client-side sem sinks. Fechado **1 baixo**: o email diário de alertas é um mailable **markdown** — o HTML era escapado, mas `[label](url)` no texto editável de um alerta renderizava um **link clicável** no email aos admins (primitiva de phishing interna com remetente de confiança); os segmentos com texto de utilizador passam a escapar a pontuação markdown antes do `line()` (o CommonMark remove o backslash ao renderizar — o texto exibido fica igual). +1 teste (396 no total). `60f27cc`

## 2026-08-06

- 🎨 **Equipamentos: o filtro de tipo passa de chips a select (estilo dos outros filtros)** — pedido da equipa: a linha de chips (Todos/UPS/Gerador/…) sai e o **tipo passa a um select** na mesma linha da pesquisa, com o mesmo estilo de campo dos filtros de família/banco — os filtros ficam todos iguais e numa só linha. Sem migração e sem build. `ae39108`
- 🧰 **Equipamentos: "mais recentes" pela ordem do PHC (não pela ordem de inserção na app)** — pedido da equipa: o backfill dos "sem cliente" inseriu equipamentos ANTIGOS do PHC por último na app, e a ordenação por defeito (id de inserção) punha-os no topo como se fossem novos. Coluna nova **`criado_erp_em`** (PHC `ma.ousrdata`+`ousrhora`, mantida alinhada pelo sync como a família) e a ordenação **recentes/antigos passa a seguir a ordem do PHC**; equipamentos manuais (sem PHC) continuam pela data de registo na app (coalesce). **Nota:** a coluna entra no hash do incremental — a primeira corrida do sync após o deploy reprocessa e preenche os ~17k (uma vez; demora uns minutos a mais). Requer migração **e** `queue:restart` (o código do sync mudou). 3 testes reforçados, 1 reescrito (395 no total). `40c51da`
- 🧹 **Despesas: saem os KPIs "Faturável à parte"/"Incluído no contrato" e o filtro "Faturável"** — pedido da equipa: desde que as despesas deixaram de se ligar a intervenções/contratos no formulário (04/08), estes cartões e o filtro só mostravam zeros e histórico — saem da listagem. Ficam **Total** e **Nº de despesas**, lado a lado; os filtros passam a período + categoria + pesquisa. A coluna `faturavel` mantém-se na BD (histórico). Sem migração e sem build. 1 teste reescrito (395 no total). `4b556c3`
- 🧰 **Scanner de recibos: deteta os 4 cantos e ENDIREITA a folha (correção de perspetiva)** — segunda afinação com fotos reais da equipa (caderno de argolas em fundo claro): dois saltos. (1) O limiar de segmentação passa a ancorar-se no **branco do próprio papel** (mediana da janela central) em vez do limiar global — fundos cinzento-claros e as argolas do caderno deixam de se colar à mancha do papel; (2) da mancha tiram-se os **4 cantos** e a folha é **endireitada por mapeamento projetivo** (correção de perspetiva com amostragem bilinear, como as apps de scanner) — uma folha inclinada ou fotografada de lado sai plana, retangular e cortada nas margens. Se o quadrilátero detetado não for coerente (área desalinhada com a mancha, lados minúsculos), cai para o recorte pela caixa aparada; sem deteção nenhuma, fica o frame inteiro. Tudo em JS puro no browser. Requer `npm run build`. 395 testes verdes. `bb19dae`
- 🧰 **Scanner de recibos: o recorte passa a assentar nas margens da folha** — afinação sobre o item abaixo, com uma digitalização real da equipa: quando o fundo também tinha zonas claras (mesa clara, outra folha atrás), a deteção arrastava essas nesgas e a caixa ficava maior que o papel — sobravam faixas escuras à volta. A caixa passa a ser **aparada por densidade**: filas e colunas de borda que não sejam maioritariamente papel (<55% de mancha) são cortadas até a borda assentar na margem da folha; se o aparo deixar uma caixa minúscula, volta ao frame inteiro. A margem final desce de 2% para 1%. Requer `npm run build`. 395 testes verdes. `0128a0e`
- 🧰 **Scanner de recibos: apanha só o papel (recorte automático) + filtro adaptativo** — pedido da equipa: o "Digitalizar" filtrava o frame inteiro da câmara — mesa, mão, tudo. A captura passa a **detetar o papel e a recortar só essa zona**: análise numa miniatura (tons de cinzento + limiar de Otsu) e enchente a partir do centro sobre os píxeis claros — a mancha clara ligada ao centro é o recibo, e a caixa dela (com 2% de margem) é o que fica; se a deteção falhar (não há papel enquadrado, ou o fundo é tão claro como o papel), mantém o frame inteiro como antes. O **filtro também deixou de ser fixo** — a curva antiga rebentava com luz forte/fraca; agora estica os níveis pelos percentis do próprio recorte, e o papel fica branco e a tinta escura em qualquer iluminação. Tudo no browser, sem bibliotecas novas. Requer `npm run build`. 395 testes verdes. `db440df`
- 🧰 **PDF das despesas: os recibos inseridos saem no documento** — pedido da equipa: os recibos digitalizados/fotografados em cada linha ficavam só na app — o PDF transferível não os mostrava. O PDF ganha a secção **"Recibos"** (em página própria, a seguir à folha): as imagens de cada linha saem **embebidas** (base64 — o dompdf tem `enable_remote=false`), 4 por fila, agrupadas por linha com dia · descrição · tipo · valor. Um ficheiro em falta no storage é saltado sem rebentar a geração; registos sem recibos ficam com o PDF exatamente como estava. Sem migração e sem build. +1 teste (395 no total). `d400c9f`
- 🧹 **Contratos: sai a "Prioridade" dos SLAs** — pedido da equipa: as linhas de SLA deixam de ser "por prioridade" (crítica/alta/normal) e passam a ser livres — **resposta em horas ou NBD + resolução + cobertura (8x5/24x7)**. Vertical completa: o select sai do editor (e deixa de ser obrigatório), a coluna sai da tabela da ficha do contrato, e o enum `PrioridadeSla` (órfão) foi removido. Migração: a coluna fica nullable e cai o unique por (contrato, prioridade) — os valores antigos ficam na BD como histórico, só deixam de se mostrar/editar. A medição de SLA em risco não muda (já usava o menor tempo de resolução do contrato, não a prioridade). Requer migração; sem build. 4 testes ajustados (394 no total). `60dfd37`
- 🧰 **Equipamentos: alertas de manutenção programados, com texto editável** — par do item abaixo, a pedido da equipa: a **ficha do equipamento** ganha a secção **"Alertas de manutenção"** — linhas com **data + texto editável** do aviso ("+ Alerta", máx. 24, botão "Guardar alertas"; o texto nasce como "Manutenção preventiva" e escreve-se o que fizer sentido — ex.: "Teste de autonomia anual", "Limpeza dos filtros"). Entram no mesmo circuito dos restantes: **painel de alertas, dashboard e email diário das 08h**, a partir de **7 dias antes da data** (média) e **alta** quando a data chega/passa; o link abre a ficha do equipamento. O alerta automático de **troca de baterias** (pela data da ficha) mantém-se como estava — isto acresce para tudo o resto. Requer migração; sem build. +2 testes (394 no total). `a36525b`
- 🧰 **Contratos: alertas de visita programados, com texto editável** — pedido da equipa: as visitas incluídas agendam-se à mão, e faltava algo que lembrasse de o fazer. O editor de contratos ganha a secção **"Alertas de visita"**: linhas com **data + texto editável** do aviso ("+ Alerta", máx. 24; o texto nasce como "Agendar visita preventiva" e escreve-se o que se quiser — ex.: "Agendar visita do 1.º trimestre"). Os alertas entram no circuito habitual — **painel de alertas, "Próximos alertas" do dashboard e email diário das 08h** — a partir de **7 dias antes da data** (severidade média) e passam a **alta** quando a data chega/passa; o link abre o editor do contrato, onde se remove a linha depois de a visita ficar agendada. Requer migração; sem build. +2 testes (392 no total). `4b092c8`
- 🧰 **Sync de equipamentos: os "sem cliente" do PHC entram na app, "por associar"** — a equipa encontrou a causa dos equipamentos em falta: no PHC há faturas lançadas **sem o nº de cliente associado** (erro humano), e o sync saltava-as por completo — o equipamento não existia na app e a pesquisa por série não o encontrava. A partir de agora: (1) esses equipamentos **entram na mesma**, sem cliente (`local_id` passa a opcional — migração); (2) na listagem aparecem com a etiqueta **"Sem cliente — por associar"** e a pesquisa por série encontra-os; (3) na **ficha**, o "Associar cliente" (a mesma pesquisa do mudar de cliente) aterra-os na Instalação principal do cliente escolhido; (4) se o cliente **aparecer na app entretanto** (sync de clientes, correção no PHC), a corrida seguinte do sync **associa-os sozinha** — os "por associar" furam o salto incremental por hash até terem local, e o coalesce nunca sobrepõe um local escolhido pelo técnico. Sem local não há cliente, logo **nunca aparecem no portal** (o isolamento resolve via local). Alertas e PDFs ficaram null-safe para o caso. **Nota:** na primeira corrida após o deploy, a contagem de equipamentos vai subir (parte do desvio de ~890 vs. PHC diagnosticado a 05/08 era isto). Requer migração; sem build. 3 testes reescritos/reforçados (390 no total). `325bae7`
- 🧹 **Listagem de equipamentos: sai o combobox "1º filtrar por cliente"** — sequela do item abaixo, a pedido da equipa: com a pesquisa a procurar sempre em todos os clientes, o combobox deixou de fazer sentido (e a navegação por cliente enganava, pelas faturas do PHC sem a série no cliente certo). Fica **só a caixa de pesquisa única** — nº de série, modelo ou nome do cliente, sempre global. Vertical completa removida (props, ações, sugestões, combobox). Sem migração e sem build. 1 teste reescrito (389 no total). `f14a195`
- 🧰 **Listagem de equipamentos: a pesquisa passa a ignorar o filtro de cliente** — a equipa percebeu a causa dos equipamentos "em falta": no PHC há faturas lançadas **sem o nº de série associado ao cliente certo** (erro humano) — dentro do cliente esperado, o equipamento nunca aparecia. Com texto na caixa de pesquisa, o **filtro de cliente deixa de restringir**: a série (ou modelo) encontra o equipamento esteja em que cliente estiver; ao limpar o texto, o filtro volta a aplicar-se. Enquanto os dois estão ativos, a UI avisa que a pesquisa está a procurar em todos os clientes. Os restantes filtros (tipo, família, banco) continuam a compor com a pesquisa. Sem migração e sem build. 1 teste reescrito (389 no total). `e285992`
- 🧹 **Varredura de código morto (janela 31/07 → 06/08)** — verificado tudo o que mudou desde a última limpeza (despesas, artigos PHC, tipos de equipamento, SADEI, dashboard): removidos os órfãos que as mudanças deixaram para trás. (1) **ServicoMetricas** perde `cumprimentoSla`, `equipamentosPorTipo/PorEstado`, `visitasPorMes` e `equipamentosSemVisitas` — ficaram sem chamadores quando os cartões/gráficos saíram do dashboard, e o `resumo()` deixava de calcular o SLA (queries + coleção inteira) a **cada render** do dashboard sem ninguém ler o resultado; (2) modelo **Despesa** perde as relações equipamento/intervenção/contrato (os campos saíram do formulário a 04/08 — as colunas e o histórico ficam na BD); (3) **RegistoDespesa** perde `anexos()` (desde a migração de 05/08 os recibos anexam-se sempre às linhas); (4) **chart.js sai do package.json/lockfile** (o import já tinha saído do bundle na limpeza do dashboard). Restante janela verificada sem órfãos (parciais Blade, scanner, trait de artigos, hooks SADEI — todos com uso real). Testes das métricas removidos com elas, 1 ajustado (389 no total). Sem migração; build local confirmou hashes idênticos — **deploy sem build**. `21e6810`
- 🔒 **Revisão de segurança (15.ª)** — auditoria ao diff desde a 14.ª (61 ficheiros: vertical das despesas — registos, recibos por linha, scanner, PDF —, catálogo de artigos do PHC, tipos novos de equipamento com descrição, hooks N\A e grelhas dinâmicas da SADEI, filtro de cliente nas listagens, dashboard novo): **sem falhas críticas, altas ou médias**. Verificado em concreto: rotas das despesas todas dentro do perímetro `papel:admin,tecnico` (clientes do portal não chegam a registos, PDFs nem recibos — o proxy de anexos também está no grupo da equipa); remoção de recibos gravados só aceita anexos de linhas do próprio registo; uploads de recibos com as regras habituais (imagem, 20 MB, 12000px, nosniff no servir); ações Livewire novas invocáveis do browser com whitelist + teto (`adicionarLinhaSadeiGrelha`, `adicionarComponenteArtigo`) e o hook `updatedFichas` só escreve em secções whitelisted do próprio estado; pesquisa de artigos parametrizada (sem SQL injection; o TOP do driver ERP é `(int)` interpolado, como os restantes); saídas novas todas escapadas (sem `{!! !!}` no intervalo); sanitização das grelhas dinâmicas da SADEI com teto de 50 linhas contra payloads forjados. Fechado **1 baixo** (defesa em profundidade): o `registoId` do editor de despesas passa a `#[Locked]` — define-se só na rota, e um payload forjado a redirecionar a gravação para outro registo a meio da sessão é recusado (mesma família do endurecimento da 11.ª). +1 teste (393 no total). `535909a`

## 2026-08-05

- 🧹 **Dashboard: saem os gráficos e os "Equipamentos sem visitas recentes"** — sequela do item abaixo, a pedido da equipa: saem do dashboard o gráfico "Visitas de contrato — planeadas vs. realizadas", os donuts "Equipamentos por tipo/estado" e o cartão "Equipamentos sem visitas recentes"; as "Renovações próximas" passam a ocupar a largura toda. O dashboard fica: KPIs → Agenda dos próximos 7 dias + Próximos alertas → Renovações. As métricas continuam todas no `ServicoMetricas` (para relatórios de gestão). Bónus: com os gráficos fora, o componente `grafico` e o **Chart.js saem do bundle** — o JS cai de ~560 kB para ~307 kB (páginas a carregar mais depressa em campo). Requer `npm run build`. 1 teste ajustado (392 no total). `6edf404`
- 🧰 **Dashboard: agenda dos próximos 7 dias + próximos alertas (e cartão de SLA removido)** — pedido da equipa: dois cartões novos logo abaixo dos KPIs, lado a lado: (1) **"Agenda — próximos 7 dias"** — eventos não cancelados a partir de hoje (título, cliente, técnico, estado; "Hoje" destacado a verde; link "Ver agenda"); (2) **"Próximos alertas"** — os alertas configurados nos equipamentos/contratos (troca de baterias, renovações, visitas em atraso, SLA em risco), os mais graves primeiro, com etiqueta Alta/Média e link direto para a ficha respetiva. O cartão **"Cumprimento de SLA" sai do dashboard** (também a pedido — a taxa continua calculada no serviço de métricas). Sem migração e sem build. +1 teste, 1 ajustado (392 no total). `380a18b`
- 🧰 **Equipamentos: "Componentes do sistema" disponível também nas UPS** — pedido da equipa: as **UPS modulares** são compostas por vários acessórios (chassis, módulos de potência 25kVA, módulos de baterias, gestão TCP/IP…) e não havia onde os listar ao registar uma UPS. A secção "Componentes do sistema" — incluindo a **pesquisa por referência do PHC** — passa a aparecer no registo de UPS (a ficha já a mostrava para todos os tipos); os bancos de baterias mantêm a secção própria. Fica de fora apenas o gerador. Sem migração e sem build. 1 teste reforçado (391 no total). `8b8897d`
- 🎨 **Medições elétricas: temperatura separada das baterias** — pedido da equipa: a caixa "Baterias / Temperatura" induzia em erro — quem lê o relatório pensava que a temperatura era a das baterias, quando é a **temperatura na UPS**. Divide-se em duas caixas: **"Baterias"** (Vbat +/Vbat −) e **"Temperatura UPS"** (Temp °C), no **formulário** da ficha de medições e no **PDF** (o alerta a vermelho acima de 25 °C mantém-se). PDFs já emitidos não são regenerados. Sem migração e sem build. 391 testes verdes. `075a8ae`
- 🎨 **Ficha SADEI: caixas de seleção OK/KO maiores no telemóvel** — pedido da equipa: em campo, os rádios pequenos ficavam demasiado perto das caixas de texto e era fácil tocar no sítio errado. Em ecrãs pequenos os **rádios passam a 20px** e cada rótulo ganha padding vertical (alvo de toque maior e mais afastado das notas); a partir de `sm` mantém-se o tamanho compacto de sempre no desktop. Aplicado às secções com nota (central/deteção/aspiração/sensores), às verificações periódicas e ao relatório final. Requer `npm run build`. 391 testes verdes. `6ea514a`
- 🎨 **Editor de relatório: marca e modelo em destaque (série no subtítulo)** — pedido da equipa: nos **separadores por equipamento** e no **cabeçalho do cartão** de cada ficha, o destaque passa a ser a **marca + modelo** (ex.: "LPG 123") em vez do nº de série; a série desce para o subtítulo do cartão e, nos separadores, fica como fallback quando o equipamento não tem marca/modelo (no cartão o fallback continua a ser o tipo real). Sem migração e sem build. 391 testes verdes. `21139ce`
- 🧹 **Ficha SADEI: campo "Prioridade" removido** — pedido da equipa: o seletor de prioridade sai do formulário da ficha SADEI (as "Recomendações e próximos passos" passam a ocupar a largura toda) e a coluna correspondente sai do **PDF** desta ficha. A ficha UPS mantém o campo; nas SADEI a prioridade fica no defeito "Normal" (fichas antigas com outra prioridade gravada mantêm o valor na BD, apenas deixa de se mostrar). Sem migração e sem build. 391 testes verdes. `7231c5f`
- 🧰 **Ficha SADEI: grelhas de cilindros sem nº fixo de linhas** — pedido da equipa: as grelhas "Cilindros — agente extintor" (4 linhas) e "Cilindros — piloto" (2) deixam de estar presas a esse número — um botão **"+ Cilindro"** em cada grelha acrescenta linhas conforme a quantidade instalada no cliente (teto de 50). Linhas vazias continuam a ser descartadas ao gravar, e **reabrir uma ficha mostra todas as linhas gravadas** (no mínimo as iniciais — antes, uma ficha com mais linhas do que o formulário mostrava escondia as restantes… situação que não podia acontecer até hoje, mas passa a poder). A sanitização aceita as linhas que vierem, com o teto como guarda contra payloads forjados. Sem migração e sem build. +2 testes (391 no total). `9de382f`
- 🧰 **Ficha SADEI: escolher o sistema de deteção preenche com N\A o que não está em uso** — sequela do item abaixo, a pedido da equipa: nos itens **Aspiração/Detecção** do "Sistema de deteção", marcar um como em uso (OK/KO) põe **o outro a N\A** (se ainda estiver por preencher) e a **secção dele toda a N\A** ("Sistema de aspiração" ↔ Aspiração; "Sistema por sensores" ↔ Detecção); marcar um diretamente a N\A preenche a própria secção. Reverter limpa apenas secções inteiramente a N\A (foi o automatismo que as preencheu) — escolhas manuais item a item nunca se perdem, e instalações com os dois sistemas continuam a poder marcar ambos. Sem migração e sem build. +1 teste (389 no total). `9ee2a47`
- 🧰 **Ficha SADEI: o tipo de manutenção preenche com N\A as verificações não aplicáveis** — pedido da equipa: escolher **Trimestral** põe automaticamente as listas "Verificação semestral" e "Verificação anual" todas a **N\A**; escolher **Semestral** põe a "Verificação anual" (Anual não preenche nada — aplica-se tudo). Alargar o período depois (ex.: trimestral → anual) **limpa o que o automatismo preencheu** — uma secção só é limpa se estiver TODA a N\A; escolhas manuais item a item nunca se perdem. O rádio do tipo passou a live (hook no componente do relatório). Sem migração e sem build. +1 teste (388 no total). `0b08792`
- 🧰 **Componentes do sistema: adicionar por referência do PHC (ou à mão, como antes)** — pedido da equipa: ao compor um sistema (registo e ficha do equipamento), a secção de componentes ganha uma **pesquisa no catálogo de artigos do PHC** — escrever a referência ou a designação lista os artigos e escolher um acrescenta a linha "REF — designação" (qtd. 1, tudo editável depois); o "+ Componente" manual mantém-se. Por baixo: o catálogo (PHC `st`) passa a ser **sincronizado para a tabela local `artigos`** (comando novo `erp:sincronizar-artigos`, encadeado no sync das 08h/13h/19h entre equipamentos e faturação, com o mesmo incremental por hash) — a pesquisa é sempre contra a BD da aplicação, o **ERP nunca está no caminho do pedido** (CLAUDE.md §5). O primeiro catálogo aparece após a primeira corrida do sync (ou um "Sincronizar PHC" no dashboard). Requer migração; sem build. +4 testes, 4 ajustados à etapa nova (387 no total). `071624f`
- 🧰 **Equipamentos: tipos novos "Monitorização ambiental" e "Diversos" (com descrição obrigatória)** — pedido da equipa: o catálogo do registo manual ganha **Monitorização ambiental** (sondas, sensores de sala técnica…) e **Diversos**, para soluções pontuais que não encaixam nos tipos existentes. Em "Diversos" aparece um **campo à frente do tipo para descrever a solução** — obrigatório (o tipo não diz nada, é a descrição que identifica; mensagem clara se faltar) e mostrado na **ficha do equipamento** junto à etiqueta do tipo; mudar de tipo depois de preencher não grava a descrição às escondidas. Ambos os tipos têm a secção **"Componentes do sistema"** (designação + quantidade), como o incêndio/sistema; os tipos novos entram sozinhos nos filtros da listagem. Sem migração e sem build. +3 testes (384 no total). `d59c2bd`
- 🎨 **Ficha do equipamento: cabeçalho duplicado removido** — sequela dos dois itens abaixo: com o cartão Identificação em destaque (modelo em primeiro, cliente/local/série logo ali), o título grande e o subtítulo da página repetiam a mesma informação. Saem por completo; as **etiquetas de tipo e estado** (que só viviam no cabeçalho) passam para o cartão Identificação, ao lado do título do cartão. A ficha abre agora diretamente no cartão. 381 testes verdes. `b2e802c`
- 🎨 **Ficha do equipamento: Identificação arrumada (modelo primeiro) e Especificações removidas** — sequela do item abaixo: (1) no cartão Identificação, o **Modelo passa para primeiro lugar**, em destaque e a toda a largura (deixa de espremer contra a coluna do meio), com o resto (nº de série, fabricante, instalação, fim de garantia) em duas colunas arrumadas por baixo; (2) a secção **"Especificações" desaparece por completo** (vertical: cartão + método `especificacoes()` do componente) — os dados em `atributos` mantêm-se na BD, apenas deixam de ter montra na ficha. 381 testes verdes. `ef46e31`
- 🎨 **Ficha do equipamento: modelo no título e Identificação em destaque** — pedido da equipa: (1) o **título troca com o modelo** — a ficha abre com "Riello UPS RIELLO SDU 6000VA…" em grande (fallback ao nº de série quando não há modelo) e o **nº de série passa ao subtítulo** (cliente · local · série); (2) o cartão **"Identificação" sobe para o topo da coluna principal** (onde estavam as Especificações), em duas colunas, com o QR — é a primeira coisa que se vê; as **Especificações passam para a lateral** em formato lista. Requer `npm run build`. 381 testes verdes. `b0a0261`
- 🎨 **Despesas: dia com calendário (sem pré-seleção), "Tipo" antes de "O que é" e obrigatórios marcados** — três afinações no registo: (1) o **Dia volta a ser calendário**, mas **nasce vazio** — nenhum dia pré-selecionado; escolher é obrigatório (erro claro por linha); (2) a ordem dos campos inverte: **Tipo antes de "O que é"** (no cartão mobile e na tabela desktop); (3) **tudo obrigatório exceto "O que é"** (marcado como opcional) — Dia, Descrição, Tipo e Valor com asterisco, A/J obrigatório nas refeições como já era. Requer `npm run build`. 1 teste reescrito (381 no total). `567699e`
- 🎨 **Despesas organizadas para o telemóvel** — os técnicos registam despesas em campo, e a grelha larga não servia num ecrã pequeno. **Editor**: em mobile cada linha passa a um **cartão empilhado** (Dia + Valor lado a lado, Descrição, O que é, Tipo com A/J, Recibos com botões de toque generosos e X de remover sempre visível nas miniaturas); a tabela no formato da folha fica para desktop (lg+); cabeçalho empilha; botões "+ Linha", Guardar e Cancelar a toda a largura. **Listagem**: filtros a ocupar a largura (grelha 2×2) e um **cartão por registo** (descrição + total em destaque, datas/colaborador/lançamentos, ações Editar/PDF/eliminar) em vez da tabela, que fica para md+. Sem mudanças de comportamento — os dois layouts ligam aos mesmos campos. Requer `npm run build`. 381 testes verdes. `b0a0261` `102c462`
- 🧰 **Despesas: linha redesenhada — dia à mão, "o que é", valor único e recibos POR LINHA** — pedido da equipa: cada linha do registo passa a ser **uma despesa completa**: **Dia escrito à mão** ("5" = dia 5 do mês atual, "04/08" ou "04/08/2026" — interpretado no servidor, ilegível é recusado), **Descrição** (cliente - localidade), **"O que é"** (campo novo `detalhe` — ex.: "Portagem A1", "Almoço com cliente"), **Tipo** (as categorias da folha, com o A/J quando é Refeições) e **Valor** único — em vez das 6 colunas de valores por linha. Os **recibos anexam-se à própria linha** (digitalizar/câmara/galeria por linha, miniaturas inline; o scanner sabe a que linha está a anexar); na edição, cada linha atualiza a MESMA despesa (id preservado → recibos sobrevivem) e linhas removidas apagam-se. O PDF mantém o layout da folha (o valor cai na coluna do tipo; a descrição leva o "o que é"). Recibos que estavam no registo passaram para a 1.ª linha. Requer migração **e** `npm run build`. Testes reescritos, +1 líquido (381 no total). `9323f11`
- 🧰 **Despesas: consolidação dos registos separados pelo backfill** — sequela do item abaixo: o backfill deu um registo a CADA despesa antiga, separando as que tinham sido lançadas juntas (o modelo anterior criava uma despesa por coluna preenchida). Migração de consolidação: despesas do **mesmo colaborador + mesma data + mesma descrição** juntam-se no registo mais antigo do grupo (recibos incluídos); os registos esvaziados desaparecem. Na listagem, o "BNP ×3" e afins passam a UMA entrada com os lançamentos dentro. Requer migração. +1 teste (381 no total). `d0da391`
- 🧰 **Despesas: registo multi-linha que aparece como UMA entrada + PDF transferível** — três pedidos da equipa num só: (1) a grelha aceita **várias linhas** ("+ Linha", máx. 31), cada uma com o seu dia, descrição e colunas — os inputs passam a ocupar a **largura toda** das células; (2) o conjunto grava como **um REGISTO** (tabela nova `registos_despesa` com o cabeçalho; cada célula continua a ser uma linha em `despesas` → os KPIs por categoria mantêm-se exatos) e a **listagem mostra uma só entrada** por registo (datas, descrição, colaborador, nº de lançamentos, total; editar/PDF/eliminar); despesas existentes foram migradas cada uma para o seu registo; (3) **"Transferir PDF"** no editor e na listagem — a folha em paisagem com o logótipo Nexus, as linhas do registo, EUROS por coluna, o A/J junto às refeições e o total (download direto). Eliminar um registo elimina as linhas (recuperável). Requer migração **e** `npm run build`. Ficheiro de testes reescrito, +2 líquidos (380 no total). `120227f`
- 🧰 **Despesas: nota a) da folha em funcionamento (A — almoço / J — jantar)** — a nota deixou de ser só texto: a célula de **Refeições** ganha um seletor **A / J** e, com valor preenchido nas refeições, escolher um passa a ser **obrigatório** (validação no servidor, com mensagem clara). O tipo grava-se apenas na despesa de Refeições (`despesas.refeicao_tipo`; as outras colunas ficam a null, e mudar o valor de coluna limpa-o) e aparece na listagem junto à categoria ("Refeições · A"). A nota b) (especificar na descrição) já era imposta pela descrição obrigatória. Requer migração. +1 teste, 2 ajustados (378 no total). `e9e50d6`

## 2026-08-04

- 🧹 **Despesas: saem do formulário a intervenção, o cliente e o "faturável à parte"** — pedido da equipa: o formulário fica só com o essencial da folha (cabeçalho, grelha de colunas, recibos). Vertical completa: comboboxes de intervenção/cliente, pesquisa server-side, herança de ligações e o checkbox saem do componente e da view. **Despesas antigas mantêm as ligações que já têm** (a edição preserva-as — só deixam de se editar aqui); despesas novas nascem sem ligações e não-faturáveis. Os KPIs da listagem (faturável/incluído) mantêm-se para o histórico. −4/+1 testes, ficheiro de testes reescrito (377 no total). `9330d27`
- 🎨 **Despesa individual no LAYOUT da folha da empresa** — pedido da equipa: o formulário de despesa passa a ser a réplica da folha Excel, mas para um registo individual. **Cabeçalho** como na folha (Nome colaborador — o utilizador autenticado —, Matrícula, Departamento, Data; matrícula/departamento gravam na despesa, via migração); **grelha** com a linha de lançamento: Descrição "(cliente - localidade)" + as colunas Veículos da empresa (Combustíveis/Outros), Hotel, Refeições a), Táxi·Comboio·Avião, Outras b); linha **EUROS** por coluna + **Total despesas**, e as notas a)/b) da folha. Preenche-se o que se aplica: **cada coluna com valor grava uma despesa dessa categoria** (a categoria deriva do índice da coluna — não é forjável), partilhando data/descrição/cabeçalho/ligações; na edição, a 1.ª coluna preenchida atualiza a despesa aberta (pode mudar de coluna) e as extra criam novas. Intervenção/cliente, faturável e recibos (digitalizar/câmara/galeria) mantêm-se. Requer migração **e** `npm run build`. +2 testes, 4 adaptados (380 no total). `fa94035`
- 🧰 **Despesas: afinal individuais, com recibos DIGITALIZADOS (scanner na app)** — correção de rumo no próprio dia (decisão da equipa): **a folha mensal sai por completo** (grelha, PDF e tabela — migração com guardas desfaz a criação; despesas lançadas pela grelha sobrevivem como individuais) e o registo volta ao formulário de **despesa individual**, que mantém as categorias fixas e ganha a secção **Recibos**: (1) **"Digitalizar"** abre a câmara dentro da própria app (getUserMedia), captura-se o recibo e é aplicado um **filtro de documento** (tons de cinzento + contraste alto — papel claro, tinta escura, aspeto de scanner) com pré-visualização e "Repetir" antes de enviar; (2) "Tirar foto" (câmara nativa) e "Galeria" continuam disponíveis; (3) os recibos ficam **pendentes no formulário e gravam-se com a despesa** (funciona na criação, antes de existir id) — miniaturas com remover, pendentes com contorno verde; na edição, os gravados removem-se um a um (ficheiro + metadados). Upload para o object storage com os limites das fotos dos relatórios. Requer migração **e** `npm run build`. −7/+1 testes (378 no total). `40aa330`
- 🧰 **Despesas: a folha é a entrada única + recibos digitalizados pela câmara** — sequela do item abaixo, a pedido da equipa: (1) o botão **"Nova despesa" dá lugar a "A minha folha deste mês"** — abre (ou cria) a folha mensal do próprio colaborador; as despesas registam-se na grelha, como na folha impressa (o editor avulso mantém-se só para editar lançamentos existentes/ligar a intervenções); (2) a folha ganha a secção **"Recibos"**: **"Tirar foto"** abre diretamente a câmara do telemóvel (sem `multiple` — a lição do iOS) e **"Galeria"** o rolo; upload imediato para o object storage (nada se perde se a folha não for gravada), miniaturas com remover, mesmos limites das fotos dos relatórios. Requer `npm run build`. +2 testes (384 no total). `94e57c8`
- 🧰 **Despesas: folha mensal por colaborador (grelha + PDF no formato da folha da empresa)** — pedido da equipa: a secção de despesas passa a espelhar a folha Excel usada até aqui. (1) **Folhas mensais**: na listagem, escolhe-se colaborador + mês e abre-se a folha — **grelha com uma linha por dia** e as colunas fixas da folha (Combustíveis, Outros (veículos), Hotel, Refeições, Táxi/Comboio/Avião, Outras despesas), descrição "(cliente - localidade)" por dia, cabeçalho com matrícula/departamento/adiantado e rodapé com totais por coluna, total, **a devolver / a receber**. Cada célula preenchida vira uma Despesa ligada à folha (entra nos KPIs como as avulsas); editar atualiza a mesma despesa, esvaziar apaga; guardar fica na página (toast). (2) **PDF da folha** em paisagem, com o **logótipo oficial Nexus**, no layout da folha impressa (cabeçalho, grelha por dia, EUROS, notas a)/b), resumo). (3) **Categorias passam a ser as 6 colunas fixas** (whitelist no servidor; as antigas foram remapeadas para "Outras despesas") — o editor de despesa avulsa troca o campo livre por um select. Requer migração **e** `npm run build`. +5 testes, 2 reescritos (382 no total). `8113641`
- 🧰 **Listagem de equipamentos: filtro por cliente antes da pesquisa** — pedido da equipa: com ~17 mil equipamentos, pesquisar por texto misturava clientes. A listagem ganha um **combobox de cliente** (pesquisa por nome sem acentos ou NIF, como nos outros) como 1º filtro — escolhido o cliente, a **pesquisa de nº de série/modelo atua só dentro dos equipamentos dele** (o placeholder passa a dizê-lo), e o campo fica realçado com um X para limpar. Escrever de novo no campo desfaz a seleção; o filtro fica no URL (partilhável). Compõe com os filtros existentes (tipo, família, banco, ordenação). Requer `npm run build`. +1 teste (377 no total). `fc7873f`
- 🎨 **PDF: medições elétricas iguais ao formulário (grelha de caixas)** — pedido da equipa: a secção das medições no PDF deixava a tabela corrida ("Tensão de entrada L/N · L1 x · L2 y…") e passa a ser o **espelho exato do formulário**: grelha de caixas 3 por linha, cada uma com o título do grupo (Entrada — Tensão L-N, Carga (%), Frequência, Saída, Correntes, Baterias/Temperatura) e os valores por baixo dos rótulos L1/L2/L3, com sublinhado por valor e "—" nos vazios. A secção passa a chamar-se "Medições elétricas" (como no formulário) e o vermelho da temperatura >25 °C mantém-se. Verificado com PDF real. PDFs já emitidos não são regenerados. 376 testes verdes (3 ajustados ao rótulo novo). `1ca84ef`
- 🧹 **Ficha de medições UPS: removida a grelha "Bancos de bateria"** — pedido da equipa: os bancos fazem parte da ficha do **equipamento** (onde já se registam com nº de série, capacidade e próxima troca) e a grelha na ficha de medições do relatório duplicava esse registo. Sai do formulário; os "Módulos de potência" mantêm-se. Fichas antigas com bancos preenchidos continuam a mostrá-los no PDF (o campo e a secção do PDF ficam). 376 testes verdes. `cf7f302`
- 🧰 **Relatórios de contrato: o técnico escolhe os equipamentos da intervenção + morada nas listas** — pedido da equipa: (1) escolher um contrato **deixa de anexar automaticamente todos os equipamentos cobertos** — o contrato pode cobrir 20 e a intervenção ser só num. Com 2..50 equipamentos aparece a **lista de checkboxes** (nada pré-marcado; "Selecionar todos" disponível); com >50 a pesquisa, como antes; **só com 1 equipamento** (não há escolha) é anexado direto. Ao editar um rascunho, a seleção gravada aparece marcada; (2) tanto a lista como a pesquisa passam a mostrar **onde cada equipamento está instalado** (morada, com a cascata habitual: localização explícita → morada do local → sede do cliente), e os chips "Equipamentos do relatório" ganham a morada em tooltip — essencial para distinguir equipamentos iguais em sítios diferentes. Modo individual inalterado. 13 testes adaptados à nova semântica, 1 reescrito (376 no total). `a0d752c`
- 🧰 **Contratos: filtro por tipo no seletor de equipamentos (UPS / incêndio / …)** — pedido da equipa: em clientes com equipamentos de tipos diferentes, a secção "Equipamentos cobertos" ganha **chips de filtro por tipo** (Todos + os tipos que o cliente realmente tem — ex.: UPS, Deteção de incêndio), para ver e marcar só um tipo de cada vez. O **"Selecionar todos" respeita o filtro ativo** (com "UPS" marca só as UPS) e **junta à seleção existente** — nunca desmarca (para isso há o "Limpar"). Os chips só aparecem quando há 2+ tipos; mudar para um cliente sem o tipo filtrado repõe "Todos" (senão a lista ficava vazia sem razão visível). A pesquisa por nome/série/local continua a funcionar dentro do filtro. Requer `npm run build`. +3 testes (376 no total). `431b26a`
- 🧰 **Ficha do equipamento: histórico de intervenções abre o relatório respetivo** — pedido da equipa: cada linha do "Histórico de Intervenções" com relatório ligado passa a ser **clicável** (a linha inteira abre o editor do relatório), com o nº do relatório na linha ("Relatório 2026/0042", ou "em rascunho"), realce ao passar o rato e seta à direita. Intervenções sem relatório ficam como estavam (só informativas). Bónus: corrigido um caso raro nas **horas por dia da agenda** — com o campo de fim ainda vazio, `Carbon::parse('')` devolve "agora" (não falha) e podiam nascer linhas fantasma com o fim na hora atual; campos vazios passam a ser tratados como intervalo inválido (na UI real não acontecia — os dois campos vêm sempre preenchidos). +1 teste (373 no total). `a06038c`

## 2026-07-31

- 🧹 **Varredura de código morto: nada órfão** — verificado tudo o que mudou desde a última limpeza (assinaturas SADEI, fotos multi/compressão, ordenação de equipamentos/clientes, horas por dia na agenda, moradas nos contratos, 14.ª revisão): métodos, constantes (`ASSINATURA_MAX_*`, `MAX_DIAS_EVENTO`), imports, componentes Blade, funções JS (`preservarScroll`, `fotosUpload`, `assinaturaPad`) e props Livewire — todos com uso real. Únicos retoques: um comentário desatualizado no upload de fotos (dizia `wire:model`, o mecanismo passou a `$wire.uploadMultiple` com compressão) e linhas em branco duplicadas em 2 ficheiros. 372 testes verdes.
- 🔒 **Revisão de segurança (14.ª) + endurecimento** — auditoria ao diff desde a 13.ª (assinaturas SADEI, fotos multi/compressão, ordenação, horas por dia, moradas nos contratos): **sem falhas críticas/altas**. Fechados 1 médio + vários baixos: (1) **médio** — mover um relatório com evento na agenda mantinha as `horas_dias` do intervalo antigo: o evento "valia" só nos dias antigos e os dias novos ficavam invisíveis no calendário e **livres para double-booking**; agora as horas por dia são limpas ao mover (mesma guarda do reagendar) e `segmentos()` só usa horas por dia que cubram o primeiro e o último dia do evento; (2) **assinaturas**: defesa contra bomba de descompressão (PNG pequeno no disco que anuncia dimensões gigantes — limite de 4000px/lado e 4M pixéis **antes** de o DomPDF o descodificar), payload inválido passa a dar **erro visível ao técnico** (numa folha que é prova de execução, uma assinatura perdida em silêncio é pior que um erro), uma ficha **só com assinatura** deixa de ser apagada na gravação seguinte, e apagar/limpar fichas passa a apagar também os **PNGs no storage** (sem órfãos); (3) fotos com limite de dimensões (12000px) além do tamanho; limite de upload temporário do Livewire alinhado com os 20 MB da aplicação (estava nos 12 MB por defeito); (4) eventos da agenda limitados a **31 dias** (um fim forjado a anos de distância gerava milhares de linhas de horas por dia); (5) a morada de instalação sai dos componentes Livewire para o modelo (`Equipamento::localInstalacao()`) — deixa de ser ação invocável do browser (uma chamada forjada dava 500). **Infra** (sem commit): `memory_limit` do Apache em produção subido de 128M para 256M (uploads de 20 MB + PDFs com fotos). Dependências e perímetro a zero. +4 testes, 3 reforçados (372 no total).
- 🎨 **Ficha SADEI: linhas a separar cada questão das verificações periódicas** — nas listas longas (trimestral/semestral/anual), as questões ficavam encostadas umas às outras e era fácil perder a linha ao marcar OK/KO/N\A. Cada questão passa a estar separada por uma **linha horizontal** (divisórias no tom da borda do tema). Requer `npm run build`. 366 testes verdes. `ec5deb2`
- 🧰 **Equipamentos e clientes abrem pelos mais recentes (+ ordenação nos equipamentos)** — a listagem de **equipamentos** não tinha ordenação nenhuma: vinha pela ordem de inserção do ERP, o que enterrava no fim quem acabava de ser sincronizado ou registado. Ganha agora o mesmo seletor "Ordenar" da lista de clientes — **mais recentes** (novo defeito), mais antigos (a ordem de sempre, agora explícita), nº de série A→Z / Z→A e cliente A→Z / Z→A (sem acentos, por subquery). A lista de **clientes** passa também a abrir pelos **mais recentes** (era Nome A→Z; a alfabética continua a um clique). Whitelist no servidor, escolha refletida no URL e desempate estável na paginação. Requer `npm run build`. +3 testes (369 no total).
- 🎨 **Câmara: "Repetir" volta a funcionar no iPhone** — o `multiple` que tinha sido posto no botão de câmara quebrava o ecrã "Repetir / Usar foto" do iOS (com `capture`, o iOS não suporta bem a combinação). O botão de câmara volta a tirar **uma de cada vez** — repetir e usar foto funcionam, e a seguir o botão fica logo pronto para a próxima (as fotos acumulam na mesma). Várias de uma vez continua a fazer-se pela **Galeria**. Requer `npm run build`.
- 🎨 **Fotos: a página deixa de saltar para o topo ao tirar/remover** — reportado em campo: cada foto tirada (ou removida) devolvia o técnico ao topo do relatório, obrigando-o a descer outra vez para a seguinte — irritante com 5 ou 6 fotos. Causa: o re-render do Livewire remove do DOM o botão que estava em foco e o browser volta ao início. Agora a posição da página é guardada antes da ação e reposta depois do re-render: tira-se/remove-se várias seguidas sem sair do sítio. Requer `npm run build`.
- 🛠️🧰 **Upload de fotos do telemóvel falhava — corrigido na raiz** — reportado em campo ("failed" ao tirar foto, duas vezes seguidas). Causa: o **PHP em produção só aceitava 2 MB por ficheiro** (defaults `upload_max_filesize=2M` / `post_max_size=8M`) e qualquer foto de telemóvel (3–6 MB) era rejeitada **antes de chegar à aplicação** — daí a falha sem explicação. Corrigido em duas frentes: (1) **servidor** — `/etc/php/8.3/apache2/conf.d/99-nexus-uploads.ini` com 20M/60M/30 ficheiros (infra, sem commit); (2) **compressão no cliente** (CLAUDE.md §6): as fotos passam a ser redimensionadas a 1920px e recomprimidas em JPEG **no telemóvel** antes do upload (~300–600 KB em vez de 3–6 MB) — sobe muito mais depressa em 4G; imagens já pequenas e formatos que o browser não abre seguem intactos. O limite da aplicação acompanha (8 MB → 20 MB por foto). Ambos os botões (câmara e galeria) aceitam **várias fotos** e continuam a acumular seleções. Requer `npm run build`.
- 🎨 **Fotos do relatório: "Tirar foto" e "Galeria" em separado (telemóvel/tablet)** — a área de fotografias tinha um único botão, e em alguns telemóveis abria logo o seletor de ficheiros (sem passar pela câmara). Passa a ter **dois botões**: **Tirar foto** (abre a câmara traseira diretamente, `capture=environment`) e **Galeria** (escolhe do álbum, várias de uma vez). Em computador só aparece "Escolher ficheiros" — o botão de câmara é escondido (o `capture` é ignorado no desktop e só confundia). As duas entradas alimentam a mesma lista, que continua a acumular seleções.
- 🧰 **Ficha SADEI com assinatura do cliente e do técnico (iPad/caneta)** — pedido da equipa: nos relatórios de equipamentos de **deteção de incêndio**, a ficha ganha no fim dois campos de **assinatura desenhada no ecrã** (Apple Pencil, dedo ou rato — captura por *pointer events*, com o traço ajustado ao pixel-ratio para não sair serrilhado), cada uma com o **nome de quem assina** e botão de limpar. As assinaturas ficam **no fim de tudo**: são o último bloco do formulário (a seguir às recomendações) e, no PDF, o último bloco da ficha — **depois das fotografias**, como numa folha de obra em papel (nomes + data/hora por baixo). Guardadas como **PNG no object storage** — a BD fica só com a `storage_key` (CLAUDE.md §2, nada de blobs); o data URI vindo do browser é validado no servidor (PNG real, ≤300 KB) e um payload inválido é ignorado sem tocar na assinatura anterior; substituir uma assinatura apaga o ficheiro antigo. Requer migração **e `npm run build`**. +3 testes (366 no total).
- 🧰 **Agenda: serviços de vários dias com horas trabalhadas POR DIA (editáveis)** — refinamento do item "criação por dia" a pedido da equipa: um serviço multi-dia deixava de caber num só bloco contínuo (as noites contavam como trabalho e não havia como dizer as horas de cada dia). Agora, quando o fim é noutro dia, o formulário mostra **uma linha por dia com horas editáveis** (pré-preenchidas com as horas do intervalo) — o técnico pode voltar ao evento **em qualquer dia** e acertar as horas realmente trabalhadas nesse dia. O calendário mostra **um bloco por dia com as horas reais** (não arrastáveis — editam-se no formulário; o clique abre o mesmo evento) e a **deteção de conflitos passa a comparar as horas efetivas** (as noites/fins de tarde entre dias de serviço ficam livres para outros eventos; sobreposição com as horas trabalhadas continua bloqueada). Turnos noturnos que só cruzam a meia-noite (ex.: 22:00→06:00) continuam um bloco contínuo, como antes. Guardado em `eventos_agenda.horas_dias` (JSONB); edição pela agenda de eventos convertidos propaga como antes (início/fim reais). Requer migração **e** `npm run build`. +4 testes (363 no total). `04aa70d`

## 2026-07-29

- 🧰 **Agenda: criação por dia e registo de horas reais (mesmo em vários dias)** — pedido da equipa: (1) clicar/selecionar no calendário passa a marcar apenas o **dia** — o evento deixa de nascer colado à faixa horária onde se clicou (que obrigava a corrigir sempre a seguir); abre na hora de abertura com 1h proposta e as **horas reais escrevem-se no formulário**; (2) um evento pode **atravessar dias** (ex.: 22:00 de 2ª → 06:00 de 3ª) e, nesse caso, **deixa de ser bloqueado pelo horário de cobertura** — é registo de trabalho já feito, não planeamento. O que **não** mudou: eventos dentro do mesmo dia mantêm a regra do horário (8h–19h, dias úteis) e a **deteção de conflitos/double-booking aplica-se sempre**, incluindo aos multi-dia. Requer `npm run build`. +5 testes (359 no total).
- 🎨 **Campos obrigatórios assinalados com `*` (equipamento, contrato e relatório)** — os três formulários de criação passam a marcar com asterisco vermelho **todos** os campos que a validação exige, com legenda no topo. **Contrato** (não tinha nenhum): nº, cliente, datas de início/fim, tipo, modelo de faturação, aviso de renovação + nota nos SLAs (prioridade e cobertura obrigatórias em cada linha adicionada). **Equipamento**: juntou-se o Estado aos já marcados (cliente, tipo). **Relatório**: juntaram-se as datas e os técnicos, com a legenda a explicar que os `*` são **para finalizar** — o rascunho só precisa do equipamento. Requisitos inalterados (o nº de série continua opcional de propósito: sistemas de incêndio não têm). Requer `npm run build`. +3 testes que garantem que cada `*` corresponde a uma regra real (355 no total).
- 🧰 **Contratos: os equipamentos mostram onde cada um está instalado** — na secção "Equipamentos cobertos", tanto **no editor** (ao escolher) como **na ficha do contrato** (ao consultar), cada equipamento passa a mostrar a **morada de instalação** (com ícone de localização) em vez do nome do local, que era quase sempre "Instalação principal" e não dizia nada. Mesma cascata da ficha de medições: localização explícita do equipamento → morada do local → morada da sede do cliente (ERP) → nome do local. A **pesquisa passa a filtrar também por local** (além de nome e nº de série) e, sem marca/modelo, o título cai no tipo do equipamento em vez de ficar vazio. Requer `npm run build`. +2 testes (350 no total).
- 🧹 **Varredura de código morto: nada órfão** — imports (`app/`, `tests/`, `database/`), views/componentes Blade (incl. os novos da ficha SADEI), métodos Livewire, constantes `SADEI_*`, eventos e mensagens flash — todos com uso. Único ajuste: `FichaMedicao::sadeiVazia()` e `sadeiAtributos()` passam a `private` (só têm chamadores dentro do próprio modelo). 348 testes verdes.
- 🔒 **Revisão de segurança (13.ª) + endurecimento** — auditoria à vertical SADEI e ao fix do tipo: **sem falhas críticas/altas/médias** (whitelist de chaves/estados sólida — o output é reconstruído do esqueleto, chaves injetadas nunca chegam ao JSONB; formulário e PDF todos escapados; migração inócua). Fechados 2 baixos + 3 infos: (1) **strings das fichas truncadas a 2000 chars** (notas forjadas de MBs inchavam a BD e o job do PDF — dívida partilhada com as colunas UPS, fechada nas duas); (2) **payloads com tipos errados deixam de dar 500** (sadei como string, valores não-escalares — agora descartados em silêncio); (3) o ramo do PDF decide pela **ficha** (`tipo_equipamento`) com fallback ao equipamento — imune a equipamentos entretanto apagados; (4) SADEI forjado em fichas de equipamentos não-incêndio é descartado (regra das secções escondidas); (5) valores "0" deixam de ser descartados pelo filtro. Dependências e perímetro a zero. +1 teste, 1 reforçado (348 no total).
- 🎨 **Relatórios: equipamentos de incêndio deixavam de aparecer como "UPS"** — dois restos do tempo em que só havia UPS: (1) no cabeçalho da aba do equipamento, sem marca/modelo o fallback era o texto fixo "UPS" — passa a ser o **tipo real** ("Deteção de incêndio", "Gerador", …); (2) a ficha de medições gravava `tipo_equipamento='ups'` hardcoded — passa a registar o tipo verdadeiro do equipamento. 347 testes verdes (+1 asserção).
- 🧰 **Relatórios de incêndio com Ficha de Verificações SADEI própria** — nos relatórios, os equipamentos do tipo **"Deteção de incêndio"** deixam a ficha de medições da UPS e ganham a **ficha SADEI** (espelho da folha oficial Nexus 2024): tipo de manutenção (trimestral/semestral/anual), Central de deteção e extinção (8 itens OK/KO + notas), Sistemas de deteção/aspiração/sensores (OK/KO/N\A + notas, nº de sensores), as **verificações periódicas completas** (6 trimestrais, 18 semestrais, 10 anuais, com o aviso "inibir o sistema antes de iniciar"), dados dos cilindros (agente extintor ×4 + piloto ×2: identificação, pressão, data de carga, quantidade, peso, OK/KO) e o relatório final ("em automático, solenoide colocada, a funcionar") + notas e recomendações. **No PDF sai a folha SADEI** em vez da de medições. Os restantes tipos mantêm a ficha UPS exatamente igual; num relatório misto (UPS + incêndio), cada equipamento leva a sua. Estados validados por whitelist no servidor (forjados descartados); guardado em `fichas_medicao.sadei` (JSONB). Requer migração **e `npm run build`** (a ficha usa classes de grelha novas — sem o build aparece desformatada; foi o que aconteceu no primeiro deploy, corrigido logo a seguir). +4 testes (347 no total). `c158633`
- 🔒 **Revisão de segurança (12.ª) + endurecimento** — auditoria ao diff desde a 11.ª (relatórios na ficha do contrato, NBD, popup ativar/suspender, secções por tipo do Davide): **sem falhas críticas/altas**. Fechados 2 médios na máquina de estados do contrato: (1) `decidirEstado` (popup) não verificava o estado atual — uma chamada forjada num contrato **Expirado/Renovado** conseguia ressuscitá-lo para Ativo/Suspenso; agora exige Rascunho no servidor; (2) o caminho rascunho → **Suspender** (popup) → **Reativar** (ficha) contornava em 3 cliques a invariante "ativo exige ≥1 equipamento" — o `reativar()` passa a verificá-la. Baixa (UX): linhas de bancos/componentes preenchidas antes de mudar o tipo deixavam a validação a rebentar em campos invisíveis — secções escondidas deixam de ser validadas (a gravação já as descartava). Confirmado limpo: hasManyThrough respeita soft-deletes, NBD com exclusividade no servidor, `tipo=pdu` forjado recusado, payload de secções escondidas descartado, dependências e perímetro a zero. +3 testes (343 no total).
- 🧰 **Criar equipamento: secções consoante o tipo + PDU fora do catálogo** — pedido da equipa: (1) as secções de baixo passam a **aparecer só quando fazem sentido para o tipo** escolhido — "Bancos de baterias" só para UPS; "Componentes do sistema" só para Deteção de incêndio e Sistema; Gerador fica só com os dados gerais e notas. A gravação re-verifica (dados preenchidos antes de mudar o tipo não gravam "escondidos"); (2) o tipo **"PDU" sai do catálogo** do registo manual (dropdown e validação) — o case mantém-se no enum, por isso equipamentos legados/do ERP com esse tipo continuam válidos nas listagens e filtros. +2 testes (340 no total). `82d5e0c`

## 2026-07-28

- 🧰 **Popup "ativar / suspender" ao guardar um contrato** — ao guardar um contrato **em rascunho** (novo ou ainda não ativado), em vez de sair logo aparece um popup: **Ativar já** / **Suspender** / **Deixar em rascunho**. O "Ativar já" respeita a regra de sempre (exige ≥1 equipamento — sem equipamentos o botão fica desativado com explicação, e o servidor re-verifica na mesma); a escolha aplica o estado e segue para a ficha. Contratos já ativos/suspensos gravam e saem como antes, sem pergunta. +4 testes, 1 ajustado (338 no total). `9e566f5`
- 🧰 **SLAs com resposta NBD (Next Business Day)** — no editor de contratos, cada linha de SLA ganha a opção **NBD** para o tempo de resposta (checkbox que desativa o campo das horas — são mutuamente exclusivos, o NBD ganha e limpa as horas ao gravar). A ficha do contrato mostra "NBD" na coluna do tempo de resposta. A resolução continua em horas (o alerta de SLA em risco usa a resolução — não muda). Requer migração (`contrato_slas.resposta_nbd`). +2 testes (334 no total). `fea206c`
- 🧰 **Ficha do contrato: secção "Relatórios do contrato"** — a ficha passa a listar os relatórios feitos no âmbito do contrato (via `intervencoes.contrato_id`, o campo que separa "incluído no contrato" de faturável à parte — CLAUDE.md §6): nº, data, equipamento, técnico, estado, e ações (PDF + Retomar/Editar). Relatórios individuais do mesmo equipamento não entram (âmbitos separados). Relação nova `Contrato::relatorios()` (hasManyThrough). +1 teste (332 no total). `940e7b0`
- 🔒 **Revisão de segurança (11.ª) + endurecimento** — auditoria aos 9 commits desde a 10.ª (enviados editáveis, mudar cliente do equipamento, resumo do sync no dashboard, morada na ficha, remoção das especificações): **sem falhas críticas/altas**. Fechados 2 médios + 2 baixos: (1) **o portal do cliente passa a mostrar SÓ relatórios ENVIADOS** (listagem, dashboard e PDF — 404 nos restantes): com os enviados editáveis, o portal servia versões a meio da edição como documento oficial (a rota do PDF até regenerava o ficheiro on-demand); o aviso do editor foi corrigido em conformidade ("deixa de estar visível no portal até reenviar; a cópia por email mantém-se"); (2) **mudar o cliente do equipamento avisa da transferência do histórico** (as intervenções/relatórios passam a resolver no portal do cliente novo) e fica **auditado no log** (quem, de/para); (3) props do poll do sync com `#[Locked]` (um payload forjado provocava 500 via `Carbon::parse`) e comparação de timestamps por Carbon (não lexicográfica — mudança de hora); (4) dependências e perímetro limpos. +2 testes líquidos, 3 revistos (331 no total).
- 🧰 **Dashboard: corrigido 500 no primeiro clique do "Sincronizar PHC"** — reportado em produção: o clique disparava o sync (que correu bem), mas os pedidos de verificação seguintes rebentavam com `Public property [$] not found` — o morph do Livewire confundia os blocos condicionais novos (espera/resultado) com os vizinhos `wire:ignore` dos gráficos e enviava um payload corrompido. Corrigido com `wire:key` estáveis nos 3 ramos do banner. Quem tiver o dashboard aberto de antes do deploy deve fazer **Ctrl+F5** uma vez.
- 🧰 **Botão "Sincronizar PHC" mostra o que foi criado/atualizado** — a mensagem fixa ("iniciada em segundo plano…") dá lugar a: um estado **"a sincronizar…"** com spinner enquanto a corrida decorre (o dashboard fica a verificar de 3 em 3 s) e, quando termina (~11 s), o **resumo por etapa** — ex.: "Clientes: 0 criados, 0 atualizados, 3043 iguais · Faturação: 12 criadas…" — a verde no sucesso, a vermelho com as etapas falhadas. O job guarda o último resultado em cache (1 dia); corridas invulgarmente longas (ex.: a completa de domingo) largam a espera ao fim de 3 min com aviso. Bónus de fiabilidade: os testes do sync limpam o throttle no arranque — neste ambiente o cache dos testes é o Redis de dev e o throttle de 10 min sobrevivia entre corridas, tornando a suite intermitente. +3 testes, 1 simplificado (329 no total).
- 🧰 **Ficha do equipamento: mudar o cliente associado (com confirmação)** — na secção "Cliente final e localização" passa a ver-se o **cliente associado** (dono no sistema) com pesquisa para o mudar (nome sem acentos ou NIF, como no registo manual). Ao escolher, aparece um **popup de confirmação** — "Atualizar a ficha do equipamento?" — com o de/para e, se o equipamento estiver ligado a contratos do cliente atual, um aviso para rever as coberturas (as ligações não são mexidas automaticamente). O equipamento aterra na "Instalação principal" do cliente novo (reutilizada se existir; criada se não — a mesma designação do sync e do registo manual, sem locais paralelos). +4 testes (326 no total). `229948c`
- 🧹 **Criar equipamento: removida a secção "Especificações (UPS)"** — pedido da equipa: saem do formulário os campos Potência (kVA), Topologia, Autonomia (min) e Firmware — vertical completa (secção da view, propriedades, validação e gravação no componente). Equipamentos existentes mantêm as especificações que já têm em `atributos` (a ficha continua a mostrá-las quando existem); bancos de baterias e componentes do sistema ficam como estavam. 1 teste ajustado (322 no total). `493df8c`
- 🎨 **Ficha de medições: "Local de instalação" passa a mostrar a morada** — o campo mostrava o nome do local (ex.: "Instalação principal"), que não diz onde o equipamento está. Passa a mostrar a **morada de instalação**, por esta ordem: localização explícita na ficha do equipamento → morada do local → morada da sede do cliente (ERP, morada · código postal) → "—". O nome do local deixa de aparecer no PDF. +1 teste (322 no total). `d753442`
- 🧰 **Relatórios enviados passam a ser editáveis** — pedido da equipa: o botão "Editar" aparece também nos relatórios ENVIADOS (antes só rascunhos/finalizados; o editor redirecionava). Regras do ciclo: ao gravar, o relatório **volta a Rascunho/Finalizado** e é preciso **Reenviar** para o cliente receber a versão nova — até lá o cliente mantém o que já recebeu (aviso amarelo bem visível no editor); o número e o histórico de envio (`enviado_em`/`enviado_para`) preservam-se; **cada reabertura de um enviado fica registada no log** (quem, quando, o quê). A etiqueta no topo do editor passa a mostrar o estado real (antes dizia sempre "Rascunho"). Eliminar enviados continua bloqueado. Nota: isto reverte deliberadamente a guarda da 8.ª revisão de segurança ("enviado não se edita") — deixou de ser bug, passou a ser feature auditada. +0 testes líquidos, 2 reescritos (321 no total).

## 2026-07-27

- 🔒 **Revisão de segurança (10.ª) + endurecimento** — auditoria a todo o diff desde a 9.ª (vertical do sync PHC, incremental por hash, fusos, ordenação dos relatórios, PDFs/agenda do Davide): **sem falhas críticas/altas/médias**. Confirmado limpo: driver SQL sem injeção (queries estáticas, `--limit` com cast), botão do sync com 3 camadas de autorização + throttle atómico, ordenação por whitelist real, PDFs escapados, cores do calendário de paleta fixa, locks da agenda intactos, expiração MFA/convites coerente com o fuso novo. Fechados 4 pontos (2 baixos + 2 DiD): (1) o **email de resultado deixa de expor detalhe técnico** em falhas (uma `QueryException` incluía host/query do SQL Server do ERP; agora o email é genérico e o detalhe fica no log); (2) hash do incremental com `JSON_INVALID_UTF8_SUBSTITUTE` (UTF-8 inválido do FreeTDS gerava hash constante → registo eternamente stale); (3) **corrida completa semanal** (domingo 06h, `--completo`) como rede de segurança contra drift/hash envenenado — o incremental removeu a "auto-cura" do sync antigo; (4) filtro `#rrggbb` nas cores do calendário no JS + fallback do logótipo no PDF se o ficheiro faltar. +2 testes, 2 revistos (321 no total).
- 🧰 **Listagem de relatórios com ordenação (padrão da lista de clientes)** — seletor "Ordenar" ao lado dos filtros: **mais recentes** (default, o comportamento de sempre), **mais antigos**, **cliente A→Z / Z→A** (ignora acentos/maiúsculas; subquery até ao cliente do relatório) e **nº de relatório** crescente/decrescente (rascunhos sem número ficam no fim). Whitelist no servidor (sem injeção), escolha refletida no URL e desempate por id (paginação estável). +2 testes (319 no total). `bcdf103`
- 🎨 **PDF do relatório: eliminada a página em branco** — sequela dos dois itens abaixo: com a linha "Equipamento / Tipo" removida, um relatório sem extras de equipamento (cliente final, componentes, "também cobertos") nem checklist legada ficava com a página técnica **vazia** — só a quebra de página — e as fichas de medição ainda quebravam para a seguinte, deixando uma página em branco entre o resumo e as fichas. A página técnica passa a só existir quando tem conteúdo. Verificado com PDF real (resumo + ficha, sem página em branco no meio). 316 testes verdes. `a0c3a34`
- 🎨 **Relatórios: PDF sem identificação do equipamento, temperatura >25 °C a vermelho e resumo que cresce com o texto** — três afinações pedidas pela equipa: (1) a linha "Equipamento / Tipo" (S/N · fabricante modelo) **sai do PDF** — quem identifica o equipamento é a ficha de medições; a secção "Equipamento" da página técnica só aparece se tiver extras (cliente final/localização, componentes, "também cobertos"); (2) a **temperatura acima de 25 °C aparece a vermelho** — no PDF (célula da ficha de medições) e no formulário (o campo fica vermelho em tempo real, ao escrever e ao carregar uma ficha gravada); no limiar (25.00) não marca; (3) o **"Resumo da intervenção" cresce com o texto** (auto-resize, sem scroll interno). Requer `npm run build`. +1 teste (316 no total). `17266a9`
- 🎨 **PDF do relatório: 1ª página só com o resumo; parte técnica na 2ª** — reorganização a pedido da equipa. A 1ª página fica só com o essencial administrativo: **cliente apenas com o nome** (saem NIF, morada, contactos e email), **"Local" passa a ser o cliente final do equipamento ou, na falta dele, a sede da empresa** (morada do ERP) — nunca o local da intervenção —, contrato, dados da intervenção e os textos. O bloco de equipamento (S/N, tipo, cliente final/localização explícitos, componentes, "também cobertos") **muda para uma página técnica que começa sempre na 2ª** (`page-break-before`), seguida da checklist e das fichas de medição (uma por página, como antes). Bónus: os textos "Problema/Trabalho realizado/Observações" passam a **respeitar as quebras de linha** escritas pelo técnico (`white-space: pre-line`). Verificado com PDF real (2 páginas). PDFs já emitidos não são regenerados. +2 testes líquidos (316 no total). `6615299`
- 🧰 **Sync de clientes lê só a SEDE (bug dos estabelecimentos do PHC)** — descoberto pelo sync incremental (178 clientes "alterados" em TODAS as corridas): no PHC o mesmo nº de cliente pode ter vários estabelecimentos (`cl.estab` — ex.: 9971 = Universidade do Porto com 11 faculdades), e como a app correlaciona por `no`, os estabelecimentos **escreviam por cima uns dos outros** a cada sync — o cliente ficava com o nome/morada do que passasse por último (a UP tanto aparecia como "Reitoria" como "Faculdade de Desporto"). A query passa a filtrar `estab = 0` (sede = registo canónico); equipamentos e faturação já correlacionavam por `no` e não mudam. Bug **pré-existente** desde o primeiro sync, invisível até agora. +1 caso de teste.
- 🧰 **Sync do PHC incremental — a faturação deixa de reescrever 191 mil linhas por corrida** — cada registo sincronizado passa a guardar a impressão digital (`hash_sync`, md5) dos dados que o ERP forneceu; na corrida seguinte, hash igual → **saltado sem nenhuma query** (o mapa id_erp→hash carrega numa só leitura). A causa dos ~20 min da faturação era dupla: 191 mil `updateOrCreate` (SELECT+UPDATE cada) e um `synced_at => now()` que tornava TODAS as linhas "sujas" mesmo sem mudanças. Agora só faturas novas/alteradas tocam na BD — o custo passa a ser essencialmente a leitura do PHC. Aplicado aos 3 syncs (clientes/equipamentos/faturação); flag `--completo` ignora os hashes (rede de segurança p/ drift local em campos do ERP, ex.: família). Equipamentos "sem cliente" não gravam hash → são re-tentados até o cliente aparecer. Requer migração; a 1.ª corrida pós-deploy ainda é lenta (popula os hashes). **Medido em produção: a corrida completa passou de ~21 minutos para ~11 segundos.** +5 testes, 1 revisto (314 no total). `f6fbebc`
- 🛠️ **Servidor de produção em hora de Lisboa** — o relógio do próprio Linux estava em UTC: `timedatectl set-timezone Europe/Lisbon` + restart do cron (para os crontabs seguirem a hora nova) + reload do Apache (logs). Efeito prático: o **backup diário passa a correr às 02:30 reais** (corria às 03:30) e todos os logs de sistema ficam na hora local. O sync já estava certo desde os fixes anteriores (o Laravel convertia o fuso) — isto alinha o resto da máquina. Infra, sem commit de código.
- 🧰 **Aplicação inteira em hora de Lisboa** — sequela do item abaixo: não eram só os agendamentos, a app toda estava em UTC (`app.timezone`), pelo que qualquer timestamp gerado pela app — criação de registos, `enviado_em` dos relatórios, logs, o "agora" do dashboard — aparecia **1h atrás do relógio real** (na hora de verão). `config/app.php` passa a `Europe/Lisbon` (mudança de hora automática). Notas de transição pontuais: os timestamps históricos ficam como estavam (naive; os antigos podem parecer 1h "mais cedo" que os novos) e um código MFA/convite emitido mesmo antes do deploy pode expirar 1h mais cedo — basta pedir outro. 309 testes verdes.
- 🧰 **Agendamentos em hora de Lisboa (o sync corria 1h atrasado)** — o servidor e a app estão em UTC, por isso as "8h/13h/19h" do sync do PHC (e as 08h do resumo de alertas) eram horas UTC: com a hora de verão, corriam na realidade às **9h/14h/20h portuguesas**. Os dois agendamentos ganham `timezone('Europe/Lisbon')` — passam a disparar às 8h/13h/19h (e 08h) **hora de Portugal, todo o ano**, com a mudança de hora tratada automaticamente. 309 testes verdes. `0fb7fde`
- 🎨 **Relatórios: removido o rótulo "principal" dos equipamentos** — ao anexar equipamentos (por cliente ou individualmente), o primeiro aparecia marcado como "principal" no chip, no separador e no cabeçalho da ficha — sem significado para o utilizador (não há hierarquia entre equipamentos do relatório). O rótulo saiu dos 3 sítios (e a frase de ajuda); internamente o 1.º continua em `intervencoes.equipamento_id` (detalhe de armazenamento) e o separador dele continua a receber as fotos antigas sem equipamento. 309 testes verdes. `8ea1434`
- 🧰 **Relatórios com data de início E de término (intervenções de vários dias)** — o formulário passa de uma só "Data de intervenção" para **início + término** (término vazio = mesmo dia; validado ≥ início, e nesse caso a hora de fim pode ser "menor" que a de início — ex.: 22:00→06:00). `intervencoes.data_fim` passa a guardar o **término real** (data do formulário + hora de fim) em vez do instante em que o relatório foi finalizado — o PDF mostra "Início 27/07/2026 · 09:30 / Término 28/07/2026 · 12:15" e a **métrica de SLA** (início→fim) passa a medir o trabalho real, não a redação. Camada 3 e edição pela agenda alinhadas (evento multi-dia ⇄ término da intervenção). +1 teste (308 no total). `40c5f13`
- 🧰 **PDF do relatório mostra o horário escrito pelo técnico** — as horas de início/fim preenchidas no formulário (`hora_inicio`/`hora_fim`) nunca chegavam ao PDF: "Data de início" mostrava a data com hora fantasma **00:00** e "Data de fim" mostrava o instante em que se *finalizou o relatório* (não o fim real da intervenção). Agora a secção Intervenção mostra **"Data da intervenção"** (d/m/Y) e **"Horário"** (ex.: 09:30 – 12:15, travessão quando não preenchido), e a ficha de medições ganha o horário junto à data. +1 teste (307 no total). `8c89b49`
- 🧰 **Agenda: removido o email ao técnico na criação de eventos** — pedido da equipa: criar (ou editar) um serviço/reunião na agenda deixa de enviar email aos técnicos envolvidos. Vertical completa: sai a notificação `EventoAtribuido`, a view do email e o cálculo de "quem notificar" no `AgendadorEvento`; comentários e testes atualizados (os testes passam a provar que **nada** é enviado). O feed iCal e o email de convite/relatórios mantêm-se. −2 testes líquidos (306 no total). `fff0079`
- 🧰 **Agenda: eventos multi-técnico divididos com a cor de cada um** — um evento com vários técnicos aparecia no calendário só com a cor do principal; agora o bloco é dividido em **faixas verticais, uma cor por técnico** (principal primeiro, adicionais por ordem alfabética, sem cores repetidas). O backend (`FonteCalendario`) envia `extendedProps.cores` e o FullCalendar pinta as faixas via `eventDidMount` (linear-gradient com paragens duras); com um só técnico nada muda. Requer `npm run build`. +1 teste (308 no total). `2fa5433`
- 🎨 **PDF do relatório com o logótipo oficial da Nexus** — o cabeçalho troca a marca em texto ("Nexus Infra" estilizado) pelo **wordmark verde oficial** (`public/img/nexus-1.png`, o mesmo da PWA), embebido como data URI (o dompdf tem `enable_remote=false` — nunca por URL), com "TECHNICAL SUITE" por baixo como na identidade da marca. Verificado em PDF gerado: imagem no topo com as proporções corretas. PDFs já emitidos não são regenerados. `55b6949`
- 🧰 **Emails do sync revistos: botão silencioso, cron reporta sempre** — o botão "Sincronizar PHC" deixa de enviar qualquer email (mesmo em erro): é só para apressar o sync sem esperar pela próxima hora; o desfecho fica no log. Em contrapartida, o sync **agendado** (08h/13h/19h) passa a enviar **sempre** o email de resultado ao suporte (`suporte@nxs.pt`, config `ERP_EMAIL_SYNC`): verde "feita com sucesso" com o resumo por etapa (criados/atualizados/erros), ou vermelho com o detalhe da falha. O Mailable `SincronizacaoErpFalhou` dá lugar ao `ResultadoSincronizacaoErp` (um só template para os dois desfechos). +1 teste líquido (307 no total). `ca0af26`
- 🧰 **Sync do PHC agendado passa a corrida encadeada** — o cron das 08h/13h/19h deixa os 3 comandos desfasados (:00/:10/:20) e passa a disparar **o mesmo job do botão** "Sincronizar PHC": clientes → equipamentos → faturação, cada etapa a arrancar quando a anterior acaba. Corrige uma sobreposição real: a faturação (~20 min, começava às :10) ainda corria quando o sync de equipamentos arrancava às :20 — duas ligações simultâneas ao PHC, precisamente o que o desfasamento queria evitar. Bónus: o lock passa a ser partilhado entre agendado e botão (nunca há dois syncs ao mesmo tempo) e **as falhas do sync agendado também passam a avisar o suporte por email**. +1 teste, 1 removido (306 no total). `3d75653`
- 🧰 **Botão "Sincronizar PHC" no dashboard (sync manual instantâneo)** — força já a sincronização de **todos** os dados do PHC (clientes → faturação → equipamentos, a mesma ordem e isolamento de falhas do agendado das 08h/13h/19h), sem esperar pelo próximo sync. Corre em **background na fila** (`Jobs\SincronizarErp`, sem retries, timeout 1700s) com lock anti-simultâneo e throttle de 10 min no botão. **Se alguma etapa falhar, o suporte é avisado por email** (`suporte@nxs.pt`, configurável via `ERP_EMAIL_FALHAS`) com a etapa e o erro — cobre também timeout/crash do worker (`failed()`). Infra: `REDIS_QUEUE_RETRY_AFTER=2100` no `.env` de produção + restart do worker (para a fila nunca re-entregar um sync a meio). +5 testes (306 no total). `8918ee6`

## 2026-07-24

- 🔒 **Revisão de segurança (9.ª)** — auditoria à remoção das ausências (`2f4fb59..f48feda`): **limpa, sem falhas de qualquer severidade**. Confirmado: nenhuma guarda remanescente enfraquecida (`abort_if` removidos pertenciam só aos métodos apagados; `ApenasEquipa` + teste por reflexão intactos), anti double-booking íntegro (advisory lock chamado nos 2 caminhos de gravação; a sobreposição de eventos nunca dependeu da tabela removida), migrações estáticas e reversíveis (`down()` fiel ao esquema original; `insertOrIgnore` idempotente), zero referências órfãs, `eventClick` do JS sem regressões (só existia o kind `evento`). Dependências limpas (`composer` + `npm audit`: 0); perímetro OK (5 headers, ficheiros sensíveis 404, TRACE 405). Único item: comentário desatualizado no `AgendadorEvento` que ainda mencionava ausências — corrigido. Nota operacional (por desenho): deixou de haver bloqueio de agendamento por férias/ausência. `f025616`
- 🧹 **Varredura de código morto pós-remoção das ausências: nada órfão** — imports de `app/`, testes e migrações todos usados; views/componentes Blade todos referenciados; métodos públicos Livewire todos com chamador; eventos `dispatch` com listener; chaves de config (`agenda`, `alertas`, `erp`) todas lidas. Único ajuste: `Relatorios\Novo::removerEquipamentoCoberto` passa de `public` a `private` (só tinha uso interno — deixa de ser invocável pelo browser, higiene de superfície). 301 testes verdes. `f48feda`
- 🧰 **Agenda: "Ausência" removida do lookup "Tipo de evento"** — sequela da remoção das ausências: a opção "Ausencia" ainda aparecia na lista pesquisável do tipo de evento (lookup `assuntos_evento`, que cresce com o uso — tinha sido criada por utilizadores). Migração de dados apaga a linha; os 6 eventos históricos criados com esse título ficam intactos no calendário. Requer migração. `a58881f`
- 🧰 **Agenda: removida a marcação de ausências** — sai tudo o que era "Marcar ausência" (pedido da equipa; a funcionalidade nunca foi usada — 0 registos em produção): o botão e o modal de marcação, o painel de detalhe/remoção, os blocos cinzentos no calendário, a verificação de conflito "técnico ausente" ao agendar, o modelo `TecnicoDisponibilidade` e a tabela `tecnico_disponibilidade` (migração de drop; `down()` recria a estrutura). A deteção de conflitos por **sobreposição de eventos** mantém-se intacta (incl. para o 2.º técnico — teste reescrito para o provar sem ausências). Requer migração. −4 testes (301 no total). `f03353d`
- 🧹 **Limpeza: 7 imports sem uso em testes** (AlertasTest ×3, ContratoTest, DashboardTest, SaldoVisitasFase1Test, SincronizarEquipamentosErpTest) — restos de refactorings. Varredura completa não encontrou mais nada morto: imports de `app/` todos usados, views/componentes Blade todos referenciados (incl. os parciais novos `pdf/_fotos` e `selecao-equipamentos`), métodos públicos dos componentes Livewire todos com chamador, eventos `dispatch` todos com listener. 305 testes verdes. `1bae2c6`
- 🔒 **Revisão de segurança (8.ª) + endurecimento** — auditoria aos 6 commits desde a 7.ª (técnicos sem pré-seleção, save de prevenção, rótulos UPS, grelha de fotos no PDF, erros visíveis ao finalizar): **as mudanças novas estão limpas** (sem `{!! !!}`, fotos do PDF como data URI server-side com mime por finfo, redirects fixos, `tecnicoIds` validado + re-filtrado, DomPDF com `enable_remote=false`). Fechados 2 pontos **pré-existentes**: (1) a guarda "relatório ENVIADO não se edita" só vivia no `mount` — `persistir` passa a re-verificar (uma chamada forjada com `intervencaoId` manipulado podia reescrever/regenerar um documento oficial já entregue; agora 403); (2) `equipamentosCobertos` passa a ser validado (`exists`, máx. 500) — antes ia direto ao `sync` (id inexistente dava 500 na FK). Dependências limpas (`composer audit` + `npm audit`: 0). Nota registada: `storage_key` no `$fillable` de `Anexo` é risco latente (hoje sem caminho de exploração). +2 testes (305 no total). `2f4fb59`
- 🧰 **"Finalizar relatório" parecia morto quando faltavam os técnicos** — desde a mudança "técnicos sem pré-seleção", finalizar sem técnicos falha a validação; mas o erro rendia no separador "Dados Gerais" e, visto de um separador de equipamento, o clique parecia não fazer nada. Agora a falha de validação é impossível de não ver: **resumo de erros sempre visível** acima dos separadores (lista os campos em falta) e **salto automático** para o separador "Dados Gerais" com scroll ao topo (evento `validacao-falhou` do Livewire → Alpine). A validação em si não mudou. +1 teste (303 no total). `40d1242`
- 🎨 **PDF: fotos deixavam de se sobrepor à secção** — as fotos no PDF eram imagens inline em sequência, e o dompdf não lhes reservava espaço vertical: sobrepunham-se à faixa verde da secção "Fotografias" (e entre filas). Passam a uma **grelha em tabela (4 por linha)** — o layout fiável do dompdf — via parcial `pdf/_fotos` usada nos 3 blocos de fotos (ficha do equipamento, equipamentos sem ficha, registo fotográfico legado). Verificado com PDF real: posições extraídas do ficheiro, zero sobreposições. PDFs já emitidos não são regenerados. `a02e012`
- 🎨 **Ficha de medição UPS: rótulos do relatório final clarificados** — "Carga a funcionar" passa a **"Equipamento a suportar a carga e sem anomalias"** e "UPS em modo normal" passa a **"Equipamento com status carga no inversor"**, no formulário da ficha e no PDF (pedido da equipa técnica — os nomes antigos eram ambíguos). Só muda o rótulo: as chaves dos dados (`carga_a_funcionar`, `ups_modo_normal`) ficam iguais, fichas antigas continuam válidas; PDFs já emitidos não são regenerados. `3d6c8a3`

- 🧰 **"Guardar rascunho" fica na página (save de prevenção)** — guardar um rascunho do relatório deixava a página e voltava à lista, obrigando a reabrir e perder o sítio do trabalho. Agora: em **edição**, fica exatamente onde está, com um toast "Rascunho guardado" (evento Livewire + Alpine); no **1.º rascunho** (vindo de /relatorios/novo), redireciona para a edição do próprio rascunho — a URL passa a apontar-lhe, pelo que um F5 retoma o rascunho em vez de abrir um formulário vazio (tudo o que estava preenchido é gravado antes, nada se perde). Finalizar mantém o regresso à lista. +2 testes (302 no total). `c3fa94c`
- 🧰 **Relatórios: técnicos sem pré-seleção e sem "principal" (quem redige ≠ quem fez)** — ao criar um relatório, o técnico logado deixa de vir pré-selecionado: quem redige pode estar a passar a limpo uma intervenção feita por outros. O campo Técnicos passa a lista de checkboxes toda selecionável (nada marcado à partida) e **sem hierarquia — deixa de existir "técnico principal"** na UI (internamente, o 1.º por ordem alfabética fica em `tecnico_id` e os restantes no pivot, mero detalhe de armazenamento; o PDF já mostrava todos numa lista única). Em edição, a seleção passa a mandar (substitui os técnicos gravados). **Finalizar passa a exigir ≥1 técnico** (o PDF identifica quem fez o trabalho); rascunhos podem ficar sem. +2 testes líquidos (300 no total). `32a6b55`
- 🎨 **Email de agendamento no tema do site** — a notificação de evento atribuído (`EventoAtribuido`) deixa o template markdown genérico do Laravel e passa a usar uma view HTML própria no tema Nexus (faixa e botão verdes, marca, rodapé — igual ao convite e ao envio de relatórios), com os detalhes do evento em caixa: quando, cliente, local, equipamento e técnicos. +1 teste (298 no total). `75b139f`

## 2026-07-23

- 🔒 **Revisão de segurança (6.ª) + endurecimento** — auditoria ao refactor da agenda e aos bancos de baterias: **sem falhas críticas/altas/médias** (advisory locks à prova de injeção e transaction-scoped, anti-loop idempotente, autorização preservada). Fechados 3 pontos de defesa em profundidade: (1) `associarBanco` deixa de permitir **cadeias de bancos** (>1 nível) por chamada forjada — um equipamento que já tem bancos não pode virar banco de outro; (2) limite de tamanho nos arrays `bancos` (máx. 50) e `componentes` (máx. 200); (3) `componentes` passa a ser validado (tipos/comprimento). +1 teste (297 no total). `d47f9e0`
- 🧹 **Agenda: refactor estrutural (fase 2)** — a lógica de domínio sai do componente Livewire (720→538 linhas) para serviços dedicados: **`SincronizadorAgenda`** é o ponto único das duas direções da sincronização agenda⇄relatórios, com guardas anti-loop **explícitas** (antes a camada 2 vivia dentro de `Calendario::criarEvento` e o anti-loop assentava na convenção "a camada 3 não chama aquele método"); **`AgendadorEvento`** concentra criar/editar/reagendar com conflitos+transação+locks; **`FonteCalendario`** serve o payload do FullCalendar e as cores/legenda; a elegibilidade de edição passa para o modelo (`EventoAgenda::editavelPelaAgenda()`). **Backfill por migração**: eventos legados com técnico só em texto ficam ligados à conta respetiva (nome normalizado, só correspondências inequívocas) — passam a contar para ausências, iCal e notificações. Sem mudança de comportamento visível; +4 testes (294 no total). Requer migração. `7179eee`

- 🧰 **Agenda: 5 correções à dinâmica** — (1) a vista passa a mostrar eventos que **atravessam** a janela visível (antes só contava quem *começava* dentro dela — um evento iniciado no mês anterior desaparecia); (2) **cores de técnico estáveis**: atribuídas por id de conta (contas novas entram no fim), em vez da posição alfabética dos nomes que baralhava as cores de toda a gente quando entrava um nome "menor"; (3) **anti double-booking**: verificação de conflitos e gravação passam a correr na mesma transação, serializada por técnico com advisory lock do Postgres (`DetetorConflitos::travarAgendaDe`) — dois utilizadores em simultâneo já não conseguem sobrepor o mesmo técnico; (4) a notificação `EventoAtribuido` passa a ir por **fila** (§12) e sai só depois da gravação; (5) **regra revista**: *finalizar* o relatório fecha o evento de agenda (Concluido = visita executada) — antes só o envio por email fechava, e visitas com entrega em mão/portal ficavam "em curso" para sempre, desvirtuando as métricas planeadas vs. realizadas (o envio continua a fechar, idempotente). +5 testes, 1 revisto (290 no total). `6eee04c`

- 🧰 **Vários bancos de baterias por equipamento** — a secção "Banco de baterias" (no criar equipamento e na ficha) passa de um banco para uma **lista** (adiciona/remove quantos quiseres), cada um com nº de série, modelo, capacidade, nº de baterias, datas. Guardado em `atributos.bancos`. Integrações preservadas: `atributos.num_baterias` fica com o **total** (a ficha de medição pré-preenche por aí) e `proxima_troca_baterias` com a data **mais próxima** (alertas de baterias a vencer). Retrocompatível com o formato antigo de um banco. +3 testes (287 no total). `ed26c32`

- 🧰 **Bancos de baterias associados aos UPS** — os bancos/kits de baterias que existem como equipamentos próprios (importados do PHC) podem agora ser **associados ao UPS a que pertencem** (`equipamento_pai_id`, auto-referência em `equipamentos`). Na ficha do UPS, a secção "Banco de baterias" mostra os bancos associados (com link) e permite associar por pesquisa server-side **por nº de série ou por local** — sem texto, sugere bancos livres no mesmo local; na ficha do banco aparece o link para o UPS pai. Guardas: um banco não pode pertencer a dois UPS (é preciso desassociá-lo primeiro) nem criar cadeias/auto-associações. Na listagem: **filtro** (com banco / sem banco / só bancos associados), etiqueta "Banco ×N" e a pesquisa passa a encontrar o UPS pelo nº de série do banco associado. Requer migração. +7 testes (285 no total). `d7dc2bf`

- 🧹 **`.gitignore`: ignora `.claude/settings.local.json`** — ficheiro de permissões locais do Claude Code (pessoal, por posto de trabalho; não deve ser versionado). `70a5534`
- 🎨 **Página "Ativos" renomeada para "Equipamentos"** — muda apenas os rótulos visíveis: menu lateral, breadcrumbs, título da listagem e subtítulo do portal do cliente ("Equipamentos cobertos sob a sua gestão."). Rotas, URLs e ids internos (`ativos`) inalterados — sem impacto funcional. 95 testes relevantes verdes. `91bbd00`
- 🔒 **Atualiza `dompdf/dompdf` 3.1.5 → 3.1.6** — corrige 6 advisories publicados a 22/07 (4 médios: leitura de ficheiros locais e oráculo de existência via SVG, DoS por bitmaps sobredimensionados; 2 baixos: oráculo via `font-face`, bypass do chroot). Relevante porque o upload de fotos aceita BMP e as imagens entram no PDF como data URI. `composer audit` limpo. 278 testes verdes. `8eb2265`

## 2026-07-20

- 🔒 **Atualiza `guzzlehttp/guzzle` 7.14.1 → 7.15.1** — corrige 4 advisories médios publicados a 20/07 (fragmentos de URI em Referer de redirects, scope de cookies host-only, cookies de resposta sem limite → DoS, headers Proxy-Authorization enviados à origem). `composer audit` limpo. 278 testes verdes. `ed87a75`

## 2026-07-17

- 🔒 **Revisão de segurança (5.ª) + endurecimento** — revisão às mudanças recentes (fotos por equipamento, técnico por conta, multi-técnico, middleware de headers) e varredura geral: **sem falhas críticas/altas/médias**. Único item de código: a resolução das contas de técnico ao gravar um evento passa a re-filtrar `papel=técnico + ativo` (defesa em profundidade, para além da validação que já bloqueava). `17dcfb9`
- 🧰 **Eventos com vários técnicos** — o campo Técnico passa a lista de checkboxes (1 ou mais, como os colaboradores do relatório): o 1.º fica como principal (cor do evento) e os restantes na pivot `evento_tecnicos`. **Todos** contam para conflitos/ausências, entram no feed iCal de cada um e são notificados; o rascunho gerado pela agenda herda os colaboradores. Requer migração. +2 testes (278 no total). `2a9e28e`
- 🧰 **Técnico dos eventos passa a ser a conta (não texto livre)** — o campo "Técnico" ao criar/editar eventos é agora a lista de contas de técnico (igual ao relatório). Com a conta ligada: **ausências/férias passam a ser detetadas** ao agendar, o evento **entra no feed iCal** do técnico, o técnico é **notificado**, e as cores ficam estáveis. Eventos legados (nome em texto): conflito detetado por nome, e ao editar o nome é casado automaticamente com a conta. +3 testes (276 no total). `2ec1f6e`
- 🧰 **Fotos do relatório por equipamento** — o carregamento de fotos deixa de ser um bloco único e passa para **dentro da ficha de cada equipamento**; cada foto guarda o `equipamento_id`. No PDF, as fotos aparecem **junto das medições** do equipamento respetivo (e um bloco no fim para equipamentos sem ficha + fotos gerais de relatórios antigos). Requer migração (`anexos.equipamento_id`). +1 teste (275 no total). `490f47e`
- 🧰 **Fotos do relatório: seleções acumulam (bug)** — adicionar uma foto e depois outra apagava a anterior (o `wire:model` do Livewire substitui o array a cada seleção). Agora cada seleção **acrescenta** (acumula em `$fotosNovas` via `updatedFotos`), e cada foto por gravar tem o seu botão de remover na pré-visualização. +1 teste (274 no total). `a371621`
- 🛠️ **Scheduler: removido o `schedule:run` duplicado do crontab do root** — corria em paralelo com o do `www-data` (redundante; a versão root podia criar ficheiros de cache/log com dono errado). Fica só o do `www-data`, que já era quem fazia os syncs. Confirmado no log do cron: pós-remoção só o www-data dispara, a cada minuto. Backup em `/root/crontab.bak-pre-limpeza-schedule`. Infra, sem commit de código.

- 🔒 **Cabeçalhos de segurança versionados (middleware)** — CSP + `nosniff`/`X-Frame`/`Referrer` passam a ser emitidos por um middleware Laravel (`CabecalhosSeguranca`), com a CSP cópia exata da de produção e um teste que garante que nunca desaparecem. Antes viviam só na config do Apache (fora do repo, não revisável, podia derivar). HSTS fica no Apache (é da camada TLS). Esses 4 headers foram removidos da config do Apache (backup `security-headers.conf.bak-pre-middleware`), ficando cada um a sair uma só vez — infra, sem commit. `c1ef8ae`
- 🔒 **`rel="noopener noreferrer"` nos links `target="_blank"`** — 4.ª revisão de segurança (PWA/frontend/geral) sem falhas críticas/altas/médias; único item de código corrigido: os 4 links que abrem em nova aba (PDFs de relatório, convite) ganham `rel="noopener"` (higiene anti reverse-tabnabbing; eram same-origin, risco já mínimo). `5715b1e`
- 🧹 **Limpeza: 3 imports `Carbon` sem uso** (Portal/Dashboard, GeradorEventoDeRelatorio, ServicoAlertas) — restos de refactorings; varredura completa não encontrou mais nada morto (imports de `app/` todos usados, assets PWA todos referenciados). 272 testes verdes. `b37282c`
- 🎨 **Ícone da app: logótipo NEXUS** — o ícone da PWA passa a ser o wordmark NEXUS a branco sobre o gradiente verde (substitui o raio genérico da primeira versão). Nota: gerado do `nexus-1.png` (150×28); com um logótipo em alta resolução/SVG pode regenerar-se mais nítido. `9fc165b`
- 🧰 **App instalável no telemóvel (PWA)** — `manifest.json` + ícones próprios (192/512/maskable + apple-touch para iOS) e metadados PWA nos layouts. Passa a poder-se "Adicionar ao ecrã principal" e abrir como app (ecrã cheio, sem barra do browser). Servido same-origin, coberto pela CSP existente. `64a20f8`
- 🎨 **Alvos de toque maiores nos modais da agenda** — os botões "✕" de fechar passam de ~20px para ~36px (mais fáceis de acertar no telemóvel). `64a20f8`
- 🎨 **Apagar fotos no telemóvel (relatórios)** — o botão de remover uma foto já carregada só aparecia com `hover`, que não existe em ecrã de toque; os técnicos não conseguiam apagar uma foto no campo. Passa a estar sempre visível, com alvo de toque maior e confirmação. +1 teste (272 no total). `4586865`
- 🎨 **Arrastar eventos por toque (mudar a hora) no telemóvel** — o arrasto vertical de um evento competia com o scroll e ficava aos saltos. Corrigido com `touch-action: none` nos eventos + toque longo mais curto (250 ms para arrastar, 500 ms para criar por seleção). Mudar o dia já funcionava; mudar a hora passa a ser fluido. Alternativa precisa: abrir o evento → Editar → hora exacta. `fc0f1ad`
- 🎨 **Modais da agenda com scroll em ecrã pequeno** — os modais (novo/editar evento, marcar ausência, detalhes) ganham altura máxima (90% do ecrã) com scroll interno. Antes, em telemóvel/ecrã baixo, o formulário passava para fora do ecrã e o botão "Guardar" ficava inalcançável. `168c58d`
- 🔒 **Endurecimento de segurança (3.ª revisão)** — revisão sem falhas críticas/altas; fechados 3 pontos de defesa em profundidade: (1) o trait `ApenasEquipa` passa a cobrir **todos** os 23 componentes de equipa (antes só 5) — um teste descobre-os automaticamente e falha se algum novo ficar sem o guard; (2) `reagendar()` na agenda ganha o seu próprio `abort_if(ehCliente)` (deixa de depender só do trait); (3) o 2.º passo do MFA volta a verificar `ativo` — uma conta desativada durante a janela do código já não completa o login. +2 testes (271 no total). `32c0134`

## 2026-07-16

- 🎨 **Cores distintas por técnico na agenda** — a cor passa a ser atribuída pela posição na lista ordenada de técnicos (1.º nome → 1.ª cor), garantindo cores diferentes até 6 técnicos. O esquema anterior (hash do nome) colidia — "Davide Fonseca" e "Rui Moreira" ficavam ambos a azul. +1 teste (269 no total). `ba77462`
- 🧰 **Editar eventos convertidos (relatório em rascunho)** — o "Editar" passa a aparecer também nos eventos que já têm intervenção, desde que o relatório ainda seja **rascunho** (o caso normal: eventos criados com equipamento convertem logo). Datas/horas propagam-se à intervenção (fonte única de verdade); equipamento e contrato ficam trancados no formulário (gerem-se no relatório). Relatórios finalizados/enviados continuam a trancar o evento. +1 teste (268 no total). `d7a4c39`
- 🧰 **Editar eventos da agenda** — novo botão "Editar" no detalhe do evento: reutiliza o formulário de criação (tipo, técnico, equipamento, contrato/cobertura, início/fim) pré-preenchido. A deteção de conflitos exclui o próprio evento. Editáveis: eventos ainda não convertidos em intervenção e não-preventivos (esses editam-se no relatório / são geridos pelo contrato). +4 testes (267 no total). `10063c5`
- 🔒 **Endurecimento de segurança (revisão)** — sem falhas críticas encontradas; corrigidos 5 pontos de defesa em profundidade: (1) o "esqueci password" deixa de revelar se um email existe (mensagem sempre neutra, mesmo em throttle); (2) o login corre sempre um `Hash::check` (contra hash dummy) para não vazar a existência de contas por timing; (3) novo trait `ApenasEquipa` que barra o papel cliente em **todas** as requisições aos componentes de equipa (Agenda, Equipamentos, Contratos), não só via middleware da rota; (4) `anexos.ver` passa a enviar `X-Content-Type-Options: nosniff` + `Content-Disposition` com nome sanitizado; (5) `SESSION_SECURE_COOKIE=true` documentado no `.env.example` e posto em produção. +3 testes (263 no total). `7fa96a4`
- 🧹 **Limpeza: enum cases inalcançáveis** — remove `EstadoIntervencao::Cancelada` e `TipoEvento::Ausencia`: nenhum fluxo da app os produz (as ausências vivem em `tecnico_disponibilidade`, não na agenda) e confirmou-se em produção que não existe nenhuma linha com esses valores (`cancelada=0`, `ausencia=0`). Zero mudanças de comportamento — 260 testes verdes. `79a0c5d`
- 🧹 **Limpeza: preview/, rotulo() e README** — apaga a pasta `preview/` (28 protótipos HTML/screenshots de junho, pré-implementação; recuperáveis no histórico do git), remove `PapelUtilizador::rotulo()` (nunca chamado) e substitui o README boilerplate do Laravel por um README real do projeto (stack, setup dev, testes, comandos, convenções). Zero mudanças de comportamento — 260 testes verdes. `3408a31`
- 🧹 **Limpeza de código morto (2.ª ronda)** — remove a dependência npm `sortablejs` (nunca importada), o método `Equipamento::cliente()` (0 chamadas), a config morta `erp.sync_hora`/`erp.connections` (+ `ERP_SYNC_HORA` do `.env.example`; a ligação real vive em `config/database.php`) e o plumbing do campo órfão `intervencoes.diagnostico` (fillable/cast — a coluna e os dados legados ficam na BD, nada os lê/escreve). Apagado também o diretório local `tools/phc_sync` (sync Python antigo, substituído pelo `erp:sincronizar-equipamentos`; não estava no git). Zero mudanças de comportamento — 260 testes verdes. `b2598b6`

## 2026-07-15

- 🧰 **Recomendações por equipamento** — o campo "Recomendações e próximos passos" (+ prioridade) deixa de ser único no relatório e passa para **cada ficha de equipamento**; sai na página da ficha respetiva no PDF. Uma recomendação, por si só, já faz a ficha persistir. Relatórios legados mantêm a recomendação antiga ("Observações"). Requer migração. `ff5f90a`
- 🎨 **Filtros dos Ativos reorganizados** — os separadores de tipo passam para uma linha própria; a pesquisa e o filtro de família ficam alinhados por baixo. Mais limpo com muitos tipos/famílias. `4e81bb6`
- 🧰 **Filtro por família nos Ativos** — traz a **família** do artigo do PHC (`st.familia`/`st.faminome`, via `ma.ref = st.ref` no sync) e acrescenta um filtro por família na listagem — para separar equipamentos (ex.: UPS) de artigos que não são equipamento (ex.: peças/reparação). Requer re-sync para popular os existentes. `b25944a`
- 🧰 **Tipo de equipamento "Sistema"** — tipo genérico para soluções compostas (ex.: WiFi por escola do Município do Barreiro). Aproveita a lista de componentes, o "sem nº de série" e a localização (a escola) já existentes. `65b3cc6`
- 🧰 **Sistema composto / deteção de incêndio** — novo tipo de equipamento "Deteção de incêndio" + **lista de componentes** (designação + quantidade) no equipamento. Permite criar contratos/relatórios para sistemas sem nº de série e compostos por várias peças (registo + ficha + PDF). `93a4854`
- 🧰 **Banco de baterias no equipamento** — secção própria (nº de série, modelo/fabricante, capacidade, nº de baterias, data de instalação, próxima troca); parte do mesmo UPS. Editável na ficha. `6c89fb3`
- 🧰 **Cliente final + localização da instalação** — campos de texto livre no equipamento (registo + ficha + PDF do relatório, com prioridade sobre a lógica derivada). `266793f`
- 🧰 **Mover equipamento + criar local na hora** — no "Alterar local", modo "Novo local" para transferir um equipamento para um cliente sem locais (mudança de titularidade). `ee88d21`
- 🧰 **Registo manual de equipamento** — equipamentos não vendidos por nós (`id_erp` nulo); ficam logo disponíveis em contratos/relatórios e o sync do ERP não lhes toca. `ece9149`
- 🧹 **Remove a entidade `Componente`** (nunca implementada) + tabela `componentes` (estava vazia). `4fb4456`

## 2026-07-14

- 🧹 **Remove métodos/relações de model sem uso** (7 itens confirmados com 0 usos). `04d7c16`
- 🧰 **Despesa ligada a intervenção** — pesquisa por nº de relatório/série/cliente; herda cliente/equipamento/contrato. `c015bd1`
- 🎨 **Logo NEXUS** no menu lateral (app + portal), em vez do texto da marca. `dbdcf37`
- 🎨 **Breadcrumb clicável** — navegar para as secções anteriores pelo topo. `ad4783d`
- 🧰 **Relatório individual por nº de série** — escreves o SN e o cliente é resolvido automaticamente. `b5e29b4`
- 🧰 **Relatórios com vários técnicos** — principal (quem cria) + colaboradores; o PDF lista todos. `6118bb8`
- 🔒 **Atualiza `guzzlehttp/guzzle` + `psr7`** — corrige 3 CVE. `0f97109`
- 🔒 **Isolamento por cliente fail-closed** — cliente sem `cliente_id` deixa de ver tudo. `54cd99c`
- 🔒 **Rate limiting no login** — 5 tentativas/60s por email+IP (anti brute-force). `337cc21`
- 🎨 **Marca renomeada** "Nexus Ops" → "Nexus Infra". `04352b9`
- 🔒 **Login em duas etapas (MFA por email)** — código de 6 dígitos, validade 10 min, 5 tentativas, cooldown de reenvio. `cdee9bf`
- 🛠️ **Hardening de produção** — headers HSTS/X-Frame/nosniff/Referrer, **CSP ativo**, `ServerTokens Prod`, `.env` 640, `MAIL_MAILER` duplicado removido, TRACE bloqueado.

## 2026-07-13

- 🛠️ **HTTPS em produção** — certificado Let's Encrypt (`infra.nexus-solutions.pt:9443`), redirect HTTP→HTTPS e bloqueio de acesso por IP (403).
- 🎨 **Correção do layout mobile** no editor de contratos. `881edcf`

---

## Pendente (ação do utilizador)
- 🔒 SSH: rotar a password do servidor + desativar login por password/root + fail2ban.
- 🛠️ Renovação do certificado Let's Encrypt (~outubro, manual — DNS-01).
